services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: koqep
      POSTGRES_PASSWORD: koqep
      POSTGRES_DB: koqep_dev
    ports:
      - '5432:5432'
    volumes:
      - koqep_postgres_data:/var/lib/postgresql/data

volumes:
  koqep_postgres_data:
