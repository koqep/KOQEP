# M6 — Legal & Launch Readiness

*Gerçek kullanıcı verisini sorumlu şekilde tutmak için gereken son parçalar: gizlilik politikası, 5651/KVKK kontrolü, izleme, yedekleme, erişilebilirlik.*

**Goal:** Close the remaining Phase 1/5 open items before pointing real acquisition efforts (the manifesto/waitlist test from `docs/review/CRITIQUE.md`'s Riskiest Assumption) at the product.
**Demo:** Privacy policy and ToS are live and linked; a simulated outage triggers an alert within minutes; a backup is restored successfully in a test environment.
**Estimated hours:** 42–68h founder-hours (revised 2026-08-09 — see "Plan notları" below: `User.totpSecret` field-level encryption + `GET /me/export` added as real gaps found by a fresh scope review; accessibility scope grew further once the actual contrast math and the repeated weak-focus-indicator pattern were found, not just the aria-* count). The 5651/KVKK legal check adds calendar time waiting on a third party, on top of these hours — see capacity check.

## Out of scope
- Anything not required to responsibly hold real user data — this is a hardening milestone, not a feature milestone.

## Acceptance criteria
- [ ] Privacy policy and ToS are published and linked from signup, and state a minimum age (KVKK's child-data provisions) — a policy line + a signup checkbox, not an ID-verification flow. Consent must be **provable** (a recorded acceptance timestamp on `User`), not just enforced client-side. The required gate does not go live in production with placeholder legal text — see Slice A.
- [ ] The 5651/KVKK legal check covers BOTH the anonymize-on-delete approach (ADR-0005, `docs/THREAT-MODEL.md` row 8) AND whether Turkish law requires connection/traffic log retention for a platform like this (today the codebase logs zero IP addresses/connection metadata, confirmed by reading the source — `docs/review/CRITIQUE.md` line 105 flags this as a separately FATAL-severity risk from the anonymization question, not the same question). Not just this review's best guess.
- [ ] An uptime check and error tracker are live and alerting the founder.
- [ ] A documented backup/restore runbook exists and has been exercised at least once — a real restore, not just a backup that's never been tested. A general "something broke at 3am, now what" runbook is written alongside it, not a separate task.
- [ ] The UI passes a real accessibility pass, not a token one: `aria-*`/semantic-role coverage across every component (3 uses exist today, all incidental/test-targeting, not deliberate work), keyboard navigability, and a **real, computed** contrast fix — not a "CRT effect" (no such effect exists in the code, confirmed by grep; that framing was wrong) but the actual default/idle text color (`text-neutral-600` on `bg-neutral-950`, ~2.53:1 computed contrast ratio, used 64 times across 13 components) failing WCAG AA outright, plus the repeated weak focus-indicator pattern (`outline-none focus:border-neutral-600`, independently duplicated in 9 components, same failing color) that strips native focus rings app-wide.
- [ ] The web app is usable at common mobile viewport widths (checked, not assumed) — no broken/overflowing layout at ~375px (`RoomHeader.tsx`'s 8+ unwrapped buttons + room tabs are the confirmed failure point), the TOTP setup QR code is actually scannable on a phone (today it renders as tiny ASCII art requiring horizontal scroll — a **functional**, not cosmetic, blocker for the device most people enable 2FA from), and at least one narrow-viewport Playwright check exists so this doesn't silently regress (zero device/viewport test coverage exists today).
- [ ] `User.totpSecret` is encrypted at rest, not stored in plaintext — `docs/THREAT-MODEL.md`'s own open item already names "M6 ships" as one of its two triggers for this; it was simply never carried into this milestone's own list until this review.
- [ ] A user can export their own data (`GET /me/export` — their messages + profile, as JSON/text; KVKK/GDPR portability, `docs/BACKLOG.md` item A8, V1-bucketed but never carried into this milestone until this review). Minimal, not a self-serve UI — matches the product's own "everything is already text" identity.
- [x] Transactional email (password reset, M1 Slice C) sends from a verified, branded domain with SPF/DKIM/DMARC configured — not Resend's shared `onboarding@resend.dev` sender. Done 2026-07-29, ahead of this milestone: `koqep.com` connected via Cloudflare, auto-configure added SPF/DKIM/DMARC, "Verified" in Resend.

## Tasks — kod dilimleri (Claude uygular, her biri kendi plan-modu turu)
- [x] **Slice A — Gizlilik/ToS sayfa iskeleti + kanıtlanabilir onay.** Tamamlandı (2026-08-10) — detay için aşağıdaki "Plan notları — Slice A uygulaması" bölümüne bakın. `/privacy`, `/terms` sayfaları (açıkça taslak işaretli placeholder içerik), `User.termsAcceptedAt` (migration), signup formunda zorunlu checkbox + linkler + server-side DTO validasyonu (`SignupDto.acceptedTerms`). **Kod tamam ama AC #1 hâlâ açık** — zorunlu kapı gerçek hukuki metin gelmeden production'a alınmayacak (aşağıya, manuel listeye bakın).
- [x] **Slice B — Hata takibi entegrasyonu + rate limit gözden geçirmesi.** Tamamlandı (2026-08-10) — detay için aşağıdaki "Plan notları — Slice B uygulaması" bölümüne bakın. Sentry (`@sentry/nestjs` + `@sentry/nextjs`), `apps/api` + `apps/web`, PII-güvenli yapılandırma (RequestData entegrasyonu `include:false` + `beforeSend`, gerçek SDK kaynağı okunarak doğrulandı). Uptime izleme bu dilimde DEĞİL (mevcut `GET /health`'e dış bir servisin bağlanması, kod değişikliği gerekmiyor — manuel listede). **Rate limit gözden geçirmesi — gerçek bir bulgu:** "5/saat davet" ÖLÜ KOD çıktı (M4 Slice B'de guard'ı silinmiş, `invites.controller.ts`'in kendi yorumu doğruluyor) — gerçek 5 aktif limit (100/60s global, 20/60s signup, 10/10s WS, 1/24s oda oluşturma, 5/saat rapor) incelendi, 20-30 kişilik ölçekte DEĞİŞTİRİLMEDİ, bilinçli karar kayda geçti.
- [x] **Slice C — Erişilebilirlik geçişi.** Tamamlandı (2026-08-11), **main'e merge edildi** (PR #52, bağımsız doğrulandı). Kontrast (`text-muted` token), odak göstergesi (`formStyles.ts`, durumlar-arası ≥3:1), composer `aria-label`'ı, `lang="en"` alt-ağaç işaretleme, `jsx-a11y` `recommended`, panel-geçişi odak yönetimi. Detay: "Plan notları — Slice C uygulaması" bölümü.
- [x] **Slice D — Mobil responsive geçiş + TOTP QR düzeltmesi + viewport test altyapısı.** Tamamlandı (2026-08-12) — detay için aşağıdaki "Plan notları — Slice D uygulaması" bölümüne bakın. `RoomHeader.tsx`/`RoomModerationSection.tsx`'e `flex-wrap` (breakpoint icat edilmedi — ajanın bulduğu gibi `max-w-2xl`/`max-w-sm` konteynerleri zaten mobil-güvenliydi, sahte alarmdı), `qrcode-terminal`→`qrcode` takası (kullanıcı onayıyla), `playwright.config.ts`'e `mobile-375` projesi + `mobile-viewport.spec.ts` (kırmızı→yeşil doğrulandı). **Henüz push/merge edilmedi** (`m6/slice-d-mobile-responsive` dalı).
- [ ] **Slice E — `User.totpSecret` alan-seviyesi şifreleme.** Yeni bir geri-döndürülebilir şifreleme yardımcı fonksiyonu (bugün `crypto.util.ts`'de sadece tek-yönlü `sha256Hex` var), yeni `TOTP_ENCRYPTION_KEY` sırrı, **geriye dönük backfill migration'ı** (production'da bugün düz-metin satırlar var — sadece yeni yazımları şifrelemek yetmez), `savePendingSecret`'ın (onay-öncesi düz-metin yazan ikinci yol) da kapsanması. Kendi başına tam bir tur, başka bir dilime katılmıyor (blast radius büyük — yanlış sıra TÜM aktif TOTP kullanıcılarını kilitler).
- [ ] **Slice F (docs) — Yedekleme/restore + genel olay runbook'u taslağı.** Claude yazar (THREAT-MODEL.md'nin zaten dağınık halde var olan manuel-SQL prosedürleri ham malzeme — ilk-kullanıcı bootstrap prosedürü dahil, bkz. THREAT-MODEL.md'nin güncellenmiş row 13 notu). Runbook'un gerçek tatbikatı manuel listede.
- [ ] **Slice G — Minimal veri dışa aktarma (`GET /me/export`).** Kullanıcının kendi mesajlarını/profilini JSON veya düz metin olarak alması — 2026-08-09 kapsam gözden geçirmesinde onaylandı.

## Tasks — founder'ın kendi eliyle yapacağı işler (kod DEĞİL, ayrı bir liste)
- [ ] **5651/KVKK hukuki kontrol.** Bir avukata ADR-0005'in anonymize-on-delete yaklaşımını VE genişletilmiş soruyu (bağlantı/trafik logu saklama zorunluluğu var mı) sordurmak. **En erken başlaması gereken iş** — üçüncü tarafa bağlı takvim süresi kod dilimleriyle paralel yürür.
- [ ] **Gizlilik Politikası + Kullanım Şartları'nın gerçek hukuki metni** (Slice A'nın iskeletine girecek) — yaş eşiği dahil (KVKK çocuk verisi hükümleri). Yaş eşiği hukuki kontrolün cevabını beklemeden uydurulmaz.
- [ ] **Render'ın auto-deploy ayarını PR merge etmeden ÖNCE doğrula (Slice A'nın planında bulundu).** `render.yaml`'da `autoDeploy` ayarı yok, CI production'a deploy yapmıyor (throwaway Postgres'e migrate ediyor) — yani "kod merge edilebilir, canlıya alınması ayrı bir karar" varsayımı SADECE Render dashboard'unda auto-deploy KAPALIYSA doğru, repo'dan doğrulanamaz. Açıksa, `m6/slice-a-terms-consent` (ve sonraki Slice A benzeri PR'lar) gerçek hukuki metin hazır olana kadar merge edilmeden taslak tutulmalı.
- [ ] **Yedeğin gerçekten var olduğunu doğrula (tatbikatın ÖN KOŞULU, 2026-08-09'da eklendi — daha önce hiç doğrulanmamıştı).** Render Postgres Basic-256mb ücretli bir plan olduğu için otomatik Point-in-Time Recovery (PITR) teorik olarak dahil (Render'ın kendi dokümantasyonu, `render.com/docs/postgresql-backups`: "Render continually backs up paid Render Postgres databases"; ayrıca 7 günlük logical backup retention, workspace planından bağımsız) — ama (a) recovery penceresi **workspace planına** bağlı (Hobby: 3 gün, Pro+: 7 gün — DB instance planından FARKLI bir ayar, hangisi kullanıldığı Render dashboard'undan doğrulanmalı), (b) PITR'ın bu SPESİFİK instance'da fiilen aktif olduğu (teorik "dahil" olması yetmez) dashboard'dan gözle doğrulanmalı. "Yedek var" varsayılmıyor, kontrol ediliyor — tatbikat bu doğrulamadan SONRA yapılır.
- [ ] **Yedekleme/restore tatbikatı** — Render Postgres konsoluna karşı gerçek bir restore denemesi (yukarıdaki doğrulama tamamlandıktan sonra).
- [ ] **Uptime izleme kurulumu** — mevcut `GET /health`'e ücretsiz bir dış servis (UptimeRobot vb.) bağlamak.
- [ ] **`TOTP_ENCRYPTION_KEY`'in Render dashboard'una elle girilmesi** (Slice E'nin bir parçası — `render.yaml` diff'i tek başına yetmez, STATE.md'nin kendi tuzağı).
- [ ] **Sentry'de ücretsiz bir org/proje oluştur, DSN'i ekle (Slice B'nin bir parçası).** `apps/api` + `apps/web` için (tek proje iki DSN ya da iki proje — tercih). `SENTRY_DSN`'i Render dashboard'una ekle; `NEXT_PUBLIC_SENTRY_DSN`'i apps/web'in gerçek deploy platformuna (muhtemelen Vercel dashboard — repo'da `vercel.json` yok, env değişkenlerinin gerçekte nerede yönetildiği doğrulanamadı) elle ekle. Deploy sonrası GERÇEK bir test hatası tetikleyip varsayılan Issue Alert e-postasının GERÇEKTEN geldiğini doğrula (kural dokümantasyonda var ama gerçek teslimat doğrulanmadı).
- [x] Verify a sending domain in Resend (SPF/DKIM/DMARC DNS records) and set `EMAIL_FROM_ADDRESS` to a branded sender — `docs/BACKLOG.md` item A11, previously flagged but unplaced; became concrete once M1 Slice C started actually sending email. Done 2026-07-29 (`koqep.com` via Cloudflare, `EMAIL_FROM_ADDRESS=noreply@koqep.com` being set in `apps/api/.env`).

## Go-live kontrol listesi

M6'nın gerçek teslimatı bu liste — dilimler bitince "hazırız" otomatik varsayılmaz, bu kapı AÇIKÇA kontrol edilir. **İlk 20-30 gerçek kullanıcı (founder'ın kendi hesabı hariç) davet edilmeden ÖNCE aşağıdakilerin HEPSİ yeşil olmalı:**

- [ ] `/privacy` ve `/terms` gerçek (taslak değil) hukuki metinle yayında, signup checkbox'ı zorunlu ve kanıtlanabilir (`User` üzerinde kayıtlı zaman damgası).
- [ ] 5651/KVKK hukuki kontrolü tamamlandı — hem anonimleştirme yaklaşımı HEM bağlantı/trafik logu saklama sorusu cevaplandı, hukuki kontrolün bulgusu varsa uygulandı (ör. gerçekten log saklama gerekiyorsa, bu kod işi launch'tan önce bitmiş olmalı).
- [ ] Hata takibi VE uptime izleme canlı, founder'a gerçek bir bildirim (email/push) test edilerek doğrulandı — sadece "kurulu" değil, "gerçekten haber veriyor."
- [ ] Yedeğin var olduğu doğrulandı (PITR aktif + doğru workspace planı) VE en az bir gerçek restore tatbikatı başarıyla tamamlandı. Genel olay runbook'u yazılı ve erişilebilir.
- [ ] Erişilebilirlik: kontrast düzeltmesi + odak göstergesi düzeltmesi + `jsx-a11y` yükseltmesi merge edildi, klavye-only bir gezinme denemesi yapıldı.
- [ ] Mobil: `RoomHeader.tsx` ~375px'te taşmıyor, TOTP QR telefondan taranabiliyor, dar-viewport Playwright testi CI'da yeşil.
- [ ] `User.totpSecret` şifreli — hem yeni yazımlar hem mevcut satırlar (backfill tamamlandı, round-trip decrypt doğrulandı), `TOTP_ENCRYPTION_KEY` Render dashboard'unda ayarlı.
- [ ] `GET /me/export` çalışıyor ve gerçek bir kullanıcı verisiyle (founder'ın kendi hesabı) uçtan uca denendi.
- [ ] Rate limit sayıları gözden geçirildi (yukarıdaki Slice B eki) — değiştirilmedi bile olsa, bilinçli bir "şu an bu sayılar yeterli" kararı kayıtlı.
- [x] E-posta gönderim domaini doğrulanmış (SPF/DKIM/DMARC) — zaten tamamlandı.

Bu liste tamamen yeşil olmadan `docs/review/CRITIQUE.md`'nin Riskiest Assumption testi (manifesto/waitlist) gerçek yabancılara açılmaz — sadece founder'ın zaten bildiği/davet ettiği kapalı çevre için M6'yı beklemeden devam etmek ayrı bir karar, bu listenin kapsamı dışında.

## Risks
- The legal check may reveal a real blocker (e.g., a stricter reading of 5651) — mitigation: start it early enough (parallel with code slices) that a finding here doesn't become a late surprise that delays launch.
- Slice E's backfill migration has real blast radius (every existing TOTP-enabled user) — mitigate with the `new-migration` skill's expand/contract discipline (write, backfill, verify round-trip decrypt, then cut over), not treated as an ordinary service-layer change.
- Slice A's required consent checkbox must not go live in production before real legal text exists behind `/privacy`/`/terms` — a required-but-uninformed consent flow is arguably worse than today's no-checkbox state under KVKK.

---

## Plan notları — 2026-08-09 kapsam gözden geçirmesi

M5 tamamen bitince M6'ya taze bir gözle geçildi — M3/M4/M5'in kullandığı aynı disiplin: milestone dosyası + çapraz referanslı dosyalar (`docs/BACKLOG.md`, `docs/THREAT-MODEL.md`, `docs/PRD.md`, ADR-0005, `docs/review/CRITIQUE.md`) taze okundu, iddialar gerçek kod okunarak (grep + doğrudan okuma + `npx eslint --print-config` + kontrast hesabı) doğrulandı, hiçbir doc claim'i sınanmadan kabul edilmedi. M6 kendi "Out of scope"unda "büyüme değil sağlamlaştırma" dediği için ölçüt M3/M4/M5'in büyüme-odaklı "Yol B"si değil: **"gerçek kullanıcı verisini tutmadan önce bunu atlamak sorumsuzluk mu?"**

Bir Plan agent'ıyla çapraz kontrol edildi (kod tabanını bağımsız okuyup 8 somut bulgu getirdi) — en kritik iddiaları (kontrast oranı, jsx-a11y'nin aktif kural sayısı, A8'in BACKLOG'da terk edilmişliği, TOTP QR'ın mobil taranamazlığı) ayrıca bağımsız doğruladım, hepsi rakamıyla eşleşti.

**Doküman çapraz-referans boşlukları (M3/M4/M5'te tekrarlayan desen — bir milestone'un kendi metni, başka bir dokümanın zaten kaydettiği bir gerekliliği hiç taşımamış):**
1. `docs/THREAT-MODEL.md` satır 39, `User.totpSecret`'ın düz metin durduğunu ve "M6 ships" ifadesini kendi somut tetikleyicilerinden biri olarak zaten yazmış — ama bu M6'nın kendi Tasks/AC'sine hiç aktarılmamıştı. Şimdi Slice E olarak eklendi.
2. `docs/BACKLOG.md` satır 331, A8 (veri dışa aktarma, KVKK/GDPR zorunluluğu, V1 bucket) 2026-07-30 LANSMAN KARARI denetiminde B1/B5/B10/A2/A3 gibi açıkça yeniden değerlendirilmedi — sessizce terk edildi. Kullanıcıya soruldu (AskUserQuestion): minimal `GET /me/export` mi, yoksa gerekçeli bir BACKLOG ertelemesi mi — **minimal endpoint** seçildi, Slice G olarak eklendi.
3. `docs/review/CRITIQUE.md` satır 105, 5651 log-saklama yükümlülüğünü GDPR/KVKK'dan AYRI, [FATAL]-seviyeli bir bulgu olarak işaretlemiş — M6'nın AC #2'si sadece ADR-0005'in anonymize-on-delete yaklaşımını adlandırıyordu, log-saklama sorusunu hiç. AC #2'nin metni genişletildi (kod tabanında bugün SIFIR IP/bağlantı logu olduğu da not edildi — hukuki kontrolün cevaplaması gereken ek bir soru, önceden inşa edilmiyor).

**Milestone metninin kendisindeki bir hata:** Kontrast maddesi "CRT-effect" diye çerçevelenmişti — `apps/web` genelinde `crt`/`scanline`/`glow` için SIFIR eşleşme, böyle bir efekt hiç yok, kod okunarak doğrulandı. Gerçek ve daha ciddi bulgu: varsayılan/idle metin rengi `text-neutral-600` (`#525252`) `bg-neutral-950` (`#0a0a0a`) üzerinde — relative luminance formülüyle hesaplanan kontrast oranı **~2.53:1**, WCAG AA'nın 4.5:1 eşiğinin ciddi altında, 13 component'te 64 kullanım (neredeyse her nav/buton/etiketin varsayılan hali). İlişkili ikinci bulgu: 9 component BAĞIMSIZ olarak aynı `outline-none focus:border-neutral-600` string'ini tanımlıyor — native focus ring'i kaldırıp AYNI başarısız rengi koyuyor, muhtemelen WCAG 2.4.7/2.4.11 ihlali, login/signup dahil her metin alanında.

**Agent'ın bulduğu, milestone metninde hiç anılmayan iki ek bulgu:**
- `jsx-a11y` zaten kurulu (Next.js'in `core-web-vitals` config'i üzerinden) ama `npx eslint --print-config` ile doğrulandı: sadece 6 dar kural aktif (`alt-text`, `aria-props`, `aria-proptypes`, `aria-unsupported-elements`, `role-has-required-aria-props`, `role-supports-aria-props`) — davranışsal kurallar (`click-events-have-key-events`, `label-has-associated-control` vb.) YOK. Lint bugün yeşil çünkü test edilecek neredeyse hiçbir `aria-*` kullanımı yok — Slice C bunu `recommended`'a yükseltip kalıcı bir regresyon kapısına çevirecek (yeni bağımlılık gerekmiyor).
- `TotpSettingsView.tsx` QR kodu `qrcode-terminal` ile ASCII sanat olarak `<pre className="overflow-x-auto text-[8px] leading-[8px]">` içinde render ediyor — yatay scroll sayfayı bozmuyor ama scroll edilerek görülen bir QR kod telefon kamerasıyla TARANAMAZ. 2FA kurulumu tanım gereği çoğunlukla authenticator'ın olduğu telefonda yapılır — bu kozmetik değil, FONKSİYONEL bir mobil engel, Slice D'ye açıkça eklendi (genel "her component'i tara" turunda kaybolmasın diye).

**Slice sıralama/kapsam kararları:**
- Slice E (totpSecret şifreleme), agent'ın bulduğu gerçek blast-radius nedeniyle kendi başına tam bir tur — `crypto.util.ts`'de bugün sadece tek-yönlü `sha256Hex` var, geri-döndürülebilir bir yardımcı fonksiyon sıfırdan yazılmalı, YENİ `TOTP_ENCRYPTION_KEY` sırrı hem koda hem `render.yaml`'a hem Render dashboard'una (STATE.md'nin kendi tuzağı — render.yaml değişiklikleri canlıya otomatik yansımıyor) elle eklenmeli, VE production'da bugün düz-metin satırlar olduğu için geriye dönük bir backfill migration gerekiyor (`savePendingSecret`'ın onay-öncesi ikinci yazım yolu dahil) — sırayı yanlış kurmak tüm aktif TOTP kullanıcılarını aynı anda kilitler.
- Slice A'nın onay kutusu KVKK anlamında "kanıtlanabilir" olmalı (yeni bir `User` kolonu, sadece frontend+DTO validasyonu değil) VE gerçek hukuki metin gelmeden production'daki zorunlu kapı AÇILMAYACAK — kod merge edilebilir, canlıya alınması ayrı bir karar.
- Hukuki kontrol (5651/KVKK) EN ERKEN başlar, kod dilimleriyle paralel yürür — milestone'un kendi Risks bölümü zaten bunu söylüyordu, değişmedi.

Doğrulama: bu tur sadece kapsam gözden geçirmesi, kod yazılmadı. Her kod diliminin (A-G) kendi implementasyonu ayrı bir plan-modu turuyla başlayacak — M3/M4/M5 ritmi.

### Sıradaki (2026-08-09'da güncellendi — bkz. aşağıdaki ikinci tur)
Kapsam gözden geçirmesi onaylandı, sonra kullanıcının dört maddelik ikinci turu işlendi. Slice A (privacy/ToS iskeleti + kanıtlanabilir onay) kendi plan-modu turuyla başlayacak.

## Plan notları — 2026-08-09 ikinci tur (kullanıcının dört maddelik gözden geçirmesi)

Kullanıcı ilk turu onayladı ama dört nokta daha buldu — hepsi `docs/BACKLOG.md` ve `docs/THREAT-MODEL.md`'nin SİSTEMATİK olarak taranmasını gerektiriyordu, ilk tur sadece totpSecret/A8 gibi tek tek örnekleri yakalamıştı.

**1-2. Ateşlenmiş tetikleyici taraması.** `docs/BACKLOG.md`'deki her "Somut tetikleyici" (7 tane) ve `docs/THREAT-MODEL.md`'nin "Open items" bölümündeki her "Concrete trigger" (6 tane) tek tek okunup değerlendirildi. Sonuç:
- **M6'ya alındı:** Rate limit sayılarının gözden geçirilmesi (`docs/milestones/M2-core-rooms-messaging.md`'nin kendi "M6 ships" tetikleyicisi — totpSecret'la BİREBİR aynı iki-taraflı stil, ilk turda gözden kaçmıştı) → Slice B'nin eki.
- **Ateşlendi ama bilinçli olarak M6'ya ALINMADI** (M6'nın kendi Out-of-scope'u — "sağlamlaştırma, özellik değil" — testini geçmedikleri için), tetikleyici metni güncellendi/kapatıldı: (a) çok-satırlı kod yapıştırma/composer `<textarea>` yükseltmesi (M3 şipti, ürün özelliği), (b) resend-verification-email endpoint'i (M3 şipti, onboarding-kolaylığı, TOTP-kilitlenme kurtarmasıyla AYNI kategoride kabul edildi), (c) raporlayana durum takibi ("M6 cila turu" ifadesi YANLIŞTI, M6 cila turu değil — düzeltildi).
- **Zaten çözülmüş, kapatıldı:** invite-issuance audit tablosu (M5 Slice E gerçekten uygulandı, ihtiyaç duymadı).
- **Stale/obsolete, doküman düzeltildi:** THREAT-MODEL.md row 13 (invite-issuance endpoint) — M2 Slice C GERÇEKTEN inşa etmişti (`POST /invites`) ama M4 Slice B SONRA tamamen kaldırmıştı (auto-grant modeline geçiş) — satır hâlâ "inşa edilecek" diye yazıyordu, iki milestone geriden. Row 35 (sıfır-kullanıcı bootstrap) aynı köke bağlıydı, düzeltildi. Row 40 (TOTP tam-kilitlenme kurtarması) "hiçbir admin/rol kavramı yok" diyordu — M5 tam olarak bunu inşa etti (`role: moderator`, `ModeratorGuard`), öncül artık yanlış, tetikleyici metni güncellendi.

**3. Yedeğin var olduğu hiç doğrulanmamıştı.** "Restore tatbikatı" manuel listedeydi ama restore edilecek bir yedek olduğu VARSAYILMIŞTI, doğrulanmamıştı. Render'ın kendi dokümantasyonu (`render.com/docs/postgresql-backups`, `render.com/pricing` — WebFetch/WebSearch ile doğrulandı) okundu: Basic-256mb ücretli bir plan olduğu için PITR teorik olarak dahil, ama recovery penceresi (Hobby: 3 gün / Pro+: 7 gün) DB planından ayrı bir "workspace planı" ayarına bağlı — hangisinin kullanıldığı ve PITR'ın bu SPESİFİK instance'da fiilen aktif olduğu hiçbir yerde doğrulanmamış. Manuel listeye tatbikattan ÖNCE gelen ayrı bir doğrulama maddesi eklendi.

**4. "Yayına hazırız" anı hiçbir yerde tanımlı değildi.** Dilimler (A-G) + manuel işler bitince otomatik "hazırız" varsaymak yerine, milestone dosyasına açık bir **"Go-live kontrol listesi"** bölümü eklendi — ilk 20-30 gerçek kullanıcıyı davet etmeden önce hepsi yeşil olması gereken somut bir liste (hukuki metin yerinde, yedek doğrulandı, izleme gerçekten bildirim gönderiyor, TOTP şifreli, export çalışıyor, rate limit kararı kayıtlı vb.).

Doğrulama: bu tur da sadece doküman — `docs/BACKLOG.md` (4 tetikleyici metni güncellendi/kapatıldı), `docs/THREAT-MODEL.md` (3 satır düzeltildi), `docs/milestones/M6-launch-readiness.md` (rate limit görevi + yedek-doğrulama maddesi + go-live listesi eklendi). Kod yazılmadı.

### Sıradaki
İkinci tur işlendi. Slice A (privacy/ToS iskeleti + kanıtlanabilir onay) kendi plan-modu turuyla başladı ve tamamlandı — detay aşağıdaki "Plan notları — Slice A uygulaması" bölümünde.

## Plan notları — Slice A uygulaması (2026-08-10)

Plan-modu turunda kod okunarak tasarlandı, bir Plan agent'ıyla çapraz kontrol edildi — gerçek bir eksik call-site ve gerçek bir operasyonel risk buldu, ikisi de işlendi.

**Agent'ın bulduğu eksik call-site:** `apps/api/test/mute.e2e-spec.ts`'in `revoke_edilmis_davet_koduyla_gercek_signup_409_doner` testi bu dosyanın KENDİ `ValidationPipe`'ı (main.ts'inkiyle birebir aynı config) üzerinden gerçek `/auth/signup`'ı çağırıyordu, `acceptedTerms` içermiyordu — `acceptedTerms` zorunlu/whitelisted olunca istek `AuthService.signup`'a hiç ulaşmadan 400 ile reddedilip test kırılacaktı. Payload'a `acceptedTerms: true` eklendi.

**Agent'ın bulduğu gerçek operasyonel risk:** `render.yaml`'da `autoDeploy` ayarı yok (grep ile doğrulandı), CI production'a deploy yapmıyor — yani "kod merge edilebilir, canlıya alınması ayrı bir karar" varsayımı SADECE Render dashboard'unda auto-deploy kapalıysa doğru, repo'dan doğrulanamaz. Yeni bir feature flag/env var İNŞA EDİLMEDİ (bu ölçekte fazla mühendislik, M6'nın kendi "sağlamlaştırma, özellik değil" duruşuyla çelişir) — bunun yerine (a) placeholder sayfalara göz ardı edilemez bir "TASLAK" banner'ı eklendi, (b) manuel listeye founder'ın Render'ın auto-deploy ayarını PR merge etmeden önce doğrulaması eklendi.

**`auth-signup-login.e2e-spec.ts`'e ValidationPipe eklenmedi, ayrı bir describe eklendi:** dosyanın ana `describe`'unun kullanıcı adı üreticisi (`` `user-${randomUUID()}` ``, 41 karakter) zaten `MAX_USERNAME_LENGTH=24`'ü aşıyor — oraya `ValidationPipe` eklemek neredeyse TÜM mevcut testleri kırardı. Bunun yerine aynı dosyaya, mevcut `describe`'a dokunmadan, kendi küçük `TestingModule`'ü + `ValidationPipe`'ı + kısa kullanıcı adlarıyla YENİ bir `describe` bloğu eklendi (`Auth signup: kanıtlanabilir onay (ValidationPipe) (e2e)`) — üç test: `acceptedTerms` eksik → 400, `false` → 400, `true` → 201 + `User.termsAcceptedAt` dolu.

**Şema:** `User.termsAcceptedAt DateTime?` — backfill yok, mevcut kullanıcılar bu onayı hiç vermedi, null kalması bunu doğru yansıtıyor (`emailVerifiedAt`/`totpEnabledAt`/`mutedUntil` ile AYNI desen).

**`auth.service.ts`:** mevcut `tx.user.create` çağrısına `termsAcceptedAt: new Date()` KOŞULSUZ eklendi — DTO validasyonu `signup()` çalışana kadar `acceptedTerms`'in `true` olduğunu zaten garanti ediyor, serviste ayrı bir dal gerekmedi.

**Frontend:** `/privacy`/`/terms` düz server component'ler (query param/etkileşim yok, `reset-password`/`verify-email`'in `Suspense`+client-View deseni gerekmedi). Yaş eşiği hiçbir yerde spesifik bir sayı olarak yazılmadı (`[YAŞ SINIRI — hukuki incelemeyi bekliyor]`). `AuthView.tsx`'e signup modunda zorunlu checkbox (native `required` + `disabled` submit butonu, `next/link` `target="_blank"` linkler — SPA'da sayfadan ayrılmak doldurulmuş formu kaybettirirdi).

Doğrulama: `apps/api` lint/typecheck/build temiz, birim (`auth.service.spec.ts`, 38 test) + e2e (`auth-signup-login.e2e-spec.ts` + `mute.e2e-spec.ts`, 20 test, iki üst üste koşu yeşil) temiz. `apps/web` lint/typecheck/build temiz, Playwright 9/9 (`auth.spec.ts` 3 yeni test + `legal-pages.spec.ts` 2 yeni test dahil). Dal `m6/slice-a-terms-consent`, push kullanıcının onayına kalıyor.

**AC #1 hâlâ açık** — bu dilim sadece iskelet, gerçek hukuki metin + Render auto-deploy doğrulaması founder'ın kendi işi (manuel listede).

### Sıradaki
Slice A (kod) tamam. Slice B (hata takibi + rate limit gözden geçirmesi) kendi plan-modu turuyla başladı ve tamamlandı — detay aşağıdaki "Plan notları — Slice B uygulaması" bölümünde.

## Plan notları — Slice B uygulaması (2026-08-10)

Plan-modu turunda kod okunarak + Sentry'nin GÜNCEL dokümantasyonu WebFetch ile taze okunarak (eğitim verisi güncel olmayabileceği için) tasarlandı, bir Plan agent'ıyla çapraz kontrol edildi — PII varsayımımdaki gerçek bir eksiği buldu, `global-error.tsx`'in ilk taslakta atlandığını yakaladı.

**Gerçek bulgu — "5/saat davet" rate limiti ölü kod, iki dokümanda hâlâ listeliydi.** `invites.controller.ts`'in kendi yorumu ("M4 Slice B: manuel oluşturma kaldırıldı ... UserThrottlerGuard gereksizleşti ve silindi") zaten var — ama `docs/STATE.md` ve bu dosyanın Slice B görev metni hâlâ "4 rate limit sayısı" diye bu ölü referansı listeliyordu, gerçekte aktif olan oda-oluşturma (1/24s) ve rapor (5/saat) guard'larını hiç saymıyordu. Gerçek aktif sayı **5** (yukarıdaki Tasks satırına yazıldı). `app.module.ts`'in kendi yorumu da AYNI hatayı taşıyordu ("davet üretme... auth.controller.ts"), düzeltildi.

**PII yapılandırması — `beforeSend` tek başına yeterli değildi, ampirik olarak doğrulandı.** Sentry'nin RequestData entegrasyonunun `include:{data,headers,cookies}` varsayılanları `sendDefaultPii`'den BAĞIMSIZ olarak `true` — `node_modules`'daki gerçek SDK kaynağı (`@sentry/core/build/cjs/integrations/requestdata.js`) okunarak doğrulandı. `Sentry.init`'te bu entegrasyon `include:false` ile AÇIKÇA yeniden yapılandırıldı. **Gerçek bir sunucu ayağa kaldırılıp kasıtlı Authorization/Cookie header'ı taşıyan bir istekle canlı bir hata tetiklendi** (geçici debug kodu, commit'e hiç girmedi) — header'lar SDK'nın internal `sdkProcessingMetadata`'sında göründü ama `@sentry/core/envelope.js`'in kaynağı okununca bu alanın transport'tan ÖNCE koşulsuz silindiği (`delete event.sdkProcessingMetadata`) doğrulandı — asıl gönderilen event zaten temiz. `beforeSend`'in `event.request` temizliği ikinci, gerçek bir savunma katmanı olarak kaldı (entegrasyon seviyesi tek başına yeterli olmayabilir).

**Agent'ın bulduğu eksik: `app/global-error.tsx`.** İlk taslağım sadece `instrumentation-client.ts`/sunucu config'leri planlamıştı — App Router'ın kök error boundary'si olmadan React render hataları Sentry'ye HİÇ ulaşmıyor. Uygulamanın kendi stiliyle (Türkçe, monospace, `bg-neutral-950`) eklendi, Sentry'nin jenerik `NextError` örneği KULLANILMADI.

**Sentry kapsamı bilerek minimal.** Source-map upload (`SENTRY_AUTH_TOKEN` + zaten oluşturulmuş bir Sentry projesi gerektiriyor, ayrıca Turbopack build'lerinde SDK'nın kendi tipine göre zaten desteklenmiyor), session replay (hata yakalamadan çok daha büyük bir PII yüzeyi) ve performans tracing'i (`tracesSampleRate: 0`) BİLEREK kurulmadı — AC #3'ün gerektirdiği "alerting" zaten Sentry'nin her yeni projede gelen varsayılan Issue Alert kuralıyla (yeni hata → email) karşılanıyor, ekstra kurulum gerekmiyor.

**`HttpException`'lar bilerek Sentry'ye gitmiyor.** `SentryGlobalFilter`'ın kendi kaynağı (`isExpectedError`) hem `HttpException` hem `WsException`/`RpcException` şeklindeki nesneleri "beklenen" sayıp yakalamıyor — bu kod tabanındaki neredeyse tüm domain hataları (`Mesaj bulunamadı: ...` vb.) bu kategoride. Sadece gerçekten beklenmeyen istisnalar Sentry'ye gidiyor, bilinçli davranış.

**`MessagesGateway`'e ayrı bir `@UseFilters(new SentryGlobalFilter())` gerekti** — `APP_FILTER` provider'ı gateway'lere UYGULANMIYOR (Sentry'nin kendi dokümantasyonu + `SentryGlobalFilter`'ın kendi `ws` context dalı okunarak doğrulandı: `WsException`'ları AYNI şekilde `client.emit('exception', ...)` ile iletiyor, davranış değişmiyor, sadece beklenmeyen hatalar da artık Sentry'ye gidiyor).

Doğrulama: `apps/api` lint/typecheck/build temiz, birim (178 test) + e2e (95 test, iki üst üste koşu) yeşil — hiçbiri Sentry değişikliğiyle kırılmadı. `apps/web` lint/typecheck/build temiz (gerçek bir Turbopack build, Sentry'nin build-zamanı zorunlu `onRouterTransitionStart` uyarısı da çözüldü) + Playwright 62/62. Dal `m6/slice-b-error-tracking`, push kullanıcının onayına kalıyor.

### Sıradaki
Slice B tamam. Slice C (erişilebilirlik geçişi) kendi plan-modu turuyla başladı ve tamamlandı — detay aşağıdaki "Plan notları — Slice C uygulaması" bölümünde.

## Plan notları — Slice C uygulaması (2026-08-11)

Plan-modu turu kod okunarak + gerçek kontrast hesaplarıyla (relative luminance formülü) + `jsx-a11y`'nin `recommended` kural setinin geçici bir test config'iyle ampirik çalıştırılmasıyla tasarlandı, bir Plan agent'ıyla çapraz kontrol edildi. **İlk `ExitPlanMode` çağrısı kullanıcı tarafından dört somut bulguyla reddedildi** — hepsi doğrulandı ve düzeltildi, ikinci turda onaylandı.

**Ret 1+4 — dil geçişi bilinçli bir karar, `DeleteAccountView.tsx` Türkçeye ÇEVRİLMEDİ.** İlk taslağım bu dosyanın İngilizce kalmasını "gözden kaçmış" sayıp Türkçeye çevirmeyi planlamıştı — `docs/milestones/M2-core-rooms-messaging.md` (satır 505-569, Slice G) ve `M2.5-identity-reliability.md` (satır 110-209) okununca bunun M2.5'te BİLEREK verilmiş bir karar olduğu doğrulandı (yeni dosyalar İngilizce, mevcut Türkçe formlar Türkçe kalıyor). Çeviri yerine `DeleteAccountView.tsx`/`VerifyEmailView.tsx`'e kendi `lang="en"` işareti eklendi (WCAG 3.1.2). Kendi tutarsızlığım da düzeltildi: `global-error.tsx` (Slice B'de bu kural bilinmeden Türkçe yazılmıştı, kendi bağımsız `<html>` kökü olduğu için) İngilizceye çevrilip `lang="en"` oldu. Kalan tam çeviri işi `docs/BACKLOG.md`'ye somut bir tetikleyiciyle (A15 — İngilizce dosya sayısı ≥8/15 olunca zorunlu, aynı eşik kök `lang` değişimi için de geçerli) eklendi, kapsamı belirsiz bırakılmadı.

**Ret 2 — odak göstergesi yanlış referans çerçevesiyle hesaplanmıştı.** WCAG 2.4.11 odaklanmış/odaklanmamış DURUMLAR ARASINDA ≥3:1 istiyor, arka plana karşı değil — ilk taslağımın `#808080`→`neutral-400` geçişi durumlar arası sadece ~1.57:1 (kullanıcının hesabıyla birebir eşleşti). Yeniden tasarlandı: `focus-visible:border-neutral-100` (durumlar arası ~3.63:1) + `ring-2 ring-offset-2` (border ile ring arasına görünür bir boşluk koyarak aynı renkte olsalar bile iki ayrı sinyal olarak okunmasını sağlıyor — kullanıcının ikinci turdaki ek notu, ilk `ring-1` (offsetsiz) önerim tek kalın çizgi gibi okunabilirdi).

**Ret 3 — composer'ın erişilebilir adı eksikti, diğer input'lar sistematik kontrol edilmedi.** `ChatPanel.tsx`'in composer'ı `placeholder`den başka erişilebilir ad taşımıyordu (WCAG 4.1.2) — `aria-label="mesaj yaz"` eklendi. Kalan 8 dosyadaki TÜM input'lar tek tek doğrulandı: hepsi ya `<label>` sarmalı ya `aria-label` taşıyor, ek düzeltme gerekmedi — composer uygulamadaki TEK adsız input'tu.

**İkinci turda kullanıcının onayla birlikte istediği üç ek:**
1. `docs/BACKLOG.md`'nin A15 satırına somut bir tetikleyici + kök `lang` değişim eşiği eklendi (yukarıda).
2. Odak göstergesinin `ring-offset` ile iki ayrı sinyale ayrılması (yukarıda).
3. `disabled:opacity-50` yeni parlaklaşan çevre metniyle (`text-muted` ~5.01:1) yan yana neredeyse görünmez kalıyordu (hesap: ~2.74:1) — WCAG disabled kontrolleri muaf tutsa da algısal bir gerileme. 8 sitede `disabled:opacity-70`'e (~4.30:1) çıkarıldı.

**Uygulama, 4 commit:** (1) `text-muted` token + `formStyles.ts` (8 dosyadaki tekrar eden `inputClassName` buraya taşındı) + `ChatPanel` odak/`aria-label` düzeltmesi + disabled opaklık, (2) `lang="en"` işaretleme + `global-error.tsx` çevirisi + BACKLOG A15, (3) `jsx-a11y` `recommended` — ampirik GERÇEK etki 5 hata, hepsi `no-autofocus`, hepsi kullanıcının kendi önceki eylemi sonucu beliren alanlar (TOTP kodu, silme onayı, mesaj/oda-adı düzenleme) — her biri kendi "neden" yorumuyla `eslint-disable-next-line` (blanket suppress yok), (4) `RoomView.tsx`'in 6 panel-view'ına (`TotpSettingsView`/`BlockedUsersView`/`InviteView`/`DeleteAccountView`/`CreateRoomView`/`ModerationQueueView`) `useFocusOnMount` hook'u — panel açılışında odak header'daki tetikleyici buttonda kalıyordu, artık panelin başlığına taşınıyor (gerçek dialog olmadıkları için `role="dialog"`/focus-trap YANLIŞ olurdu, önceki kapsam gözden geçirmesinde zaten doğrulanmıştı). Klavye-only tur: uygulamada hiçbir `<div onClick>`/`<span onClick>` deseni yok (grep ile doğrulandı) — tüm interaktif kontroller zaten gerçek `<button>`/`<input>`/`<a>`, ek bir kod değişikliği gerekmedi.

**Küçük implementasyon-sırası kararı:** onaylanan plan `docs/BACKLOG.md`'ye "B16" olarak eklenmesini öngörüyordu (B tablosu — büyüme fikirleri) ama uygulama sırasında B tablosunun içeriğinin ("herkese açık profil sayfası", "Ctrl+K komut paleti" gibi büyüme özellikleri) bu maddeyle örtüşmediği görüldü — A tablosu ("eksik zorunluluklar") içerik olarak daha uygun olduğu için **A15** olarak eklendi. İçerik/tetikleyici değişmedi, sadece doküman içindeki yeri.

Doğrulama: `apps/web` lint/typecheck/build (Turbopack) hepsi temiz, Playwright mocked e2e 62/62 geçti (panel-geçişi/odak testleri dahil). `apps/api` Jest 176/178 geçti — 2 başarısızlık `prisma.service.spec.ts`'in `localhost:5432`'de bir Postgres'e ihtiyaç duyması (bu geliştirme ortamında DB çalışmıyor), bu daldan tamamen bağımsız bir ortam kısıtı, apps/api'ye bu dilimde hiç dokunulmadı. Fullstack Playwright (`e2e-fullstack`) aynı sebeple bu ortamda çalıştırılamadı — gerçek bir Postgres olan bir ortamda (CI) ayrıca doğrulanmalı. Dal `m6/slice-c-accessibility`, 4 commit, push edildi, **main'e merge edildi (PR #52)**.

**Merge sonrası CI'da geçici bir kırmızı:** Push sonrası CI'da `delete-account.spec.ts` + `invite-issuance.spec.ts` (fullstack) iki kez kırmızı çıktı, ikisi de header butonuyla panel açan testler — Slice C'nin `useFocusOnMount` hook'una işaret ediyor gibi görünüyordu. Yerel Postgres ile CI'ın TAM adımlarıyla (migrate→seed→build→fullstack, taze DB) tekrarlanmaya çalışıldı — ikisi de TEMİZ geçti, hipotez YERELDE doğrulanamadı. Ayrıca `invite-issuance.spec.ts`'in kendi yerel koşumunda AYRI, gerçek bir bulgu ortaya çıktı: kalıcı yerel dev DB'de önceki test koşularından kalan 4 adet "seviye-atlama-*" mesajı `getByText` strict-mode ihlaline yol açıyordu (CI'ın taze/throwaway DB'sinde bu oluşamaz, tamamen yerel-ortam kirliliği, temizlenip doğrulandı). Kullanıcı tam CI hata çıktısını istenip henüz paylaşmadan "merge bitti, CI yeşildi" bilgisi geldi — muhtemelen flake'ti ya da ayrı bir düzeltme yapıldı; kök sebep kesin kanıtlanamadı, kayıtlara geçiriliyor (ileride benzer bir kırmızı çıkarsa buradan devam edilebilir).

### Sıradaki
Slice C tamam ve merge edildi. Slice D (mobil responsive geçiş + TOTP QR düzeltmesi + viewport test altyapısı) kendi plan-modu turuyla başladı ve tamamlandı — detay aşağıdaki "Plan notları — Slice D uygulaması" bölümünde.

## Plan notları — Slice D uygulaması (2026-08-12)

Plan-modu turu üç paralel Explore agent'ıyla (layout riski, QR alternatifleri, Playwright viewport altyapısı) araştırıldı, hiçbir bulgu tahmine dayanmadı.

**Bulgu — `max-w-2xl`/`max-w-sm` konteynerleri sahte alarmdı.** Agent'ın grep taraması: Tailwind `max-w-*` sadece ÜST sınır koyar, minimum genişlik dayatmaz — `max-w-sm` kullanan tüm sayfalar (Auth/VerifyEmail/ResetPassword/global-error) zaten 384px, 375px'i dolduruyor, taşma yaratmıyor; `max-w-2xl` kullananlar (RoomView kökü, privacy/terms) da kendi başına sorun değil. **Gerçek risk tamamen `RoomHeader.tsx`'in içinde**: üç flex satırı (`header`/`nav`/aksiyon `div`'i) hiçbirinde `flex-wrap` yoktu. `RoomHeader.tsx` + `RoomModerationSection.tsx`'in silme-onay satırına `flex-wrap` eklendi — YENİ bir breakpoint icat edilmedi (kod tabanında hiç emsal yoktu, `flex-wrap` hem dar viewport'ta hem "çok sayıda oda" durumunda aynı anda çalışıyor).

**Kanıtlı kırmızı→yeşil:** `mobile-viewport.spec.ts` yazıldıktan sonra `RoomHeader.tsx`'in `flex-wrap` düzeltmesi GEÇİCİ OLARAK geri alınıp test çalıştırıldı — "çıkış" butonu `toBeInViewport()` assertion'ı gerçekten KIRMIZI çıktı (`viewport ratio 0`), düzeltme geri getirilince YEŞİLE döndü. Tahmin değil, gerçek kanıt.

**Bulgu — `qrcode-terminal`'ın görüntü modu yok, `otpauth` da QR render etmiyor.** Agent'ın taraması: `apps/api`'nin `otpauth` kütüphanesi (TOTP/HOTP RFC implementasyonu) QR render yeteneği taşımıyor (tip tanımları okunarak doğrulandı), repo'da hazır kurulu bir görüntü-tabanlı QR kütüphanesi yok — gerçekten yeni bir bağımlılık gerekiyordu. Kullanıcıya soruldu (CLAUDE.md kuralı), **`qrcode` onaylandı** (tek küçük transitive bağımlılığı `dijkstrajs`, saf JS). `qrcode-terminal` + `@types/qrcode-terminal` kaldırıldı, `qrcode` + `@types/qrcode` (kendi gömülü tipi yok, ayrıca eklendi) kuruldu — net bir paket takası. `TotpSettingsView.tsx`'in `<pre>` ASCII bloğu `QRCode.toDataURL()` + `<img alt="TOTP QR kodu">` ile değiştirildi (data URL, `dangerouslySetInnerHTML` gerekmedi, XSS yüzeyi sıfır). `totp-settings.spec.ts`'in eski `<pre>` assertion'ı güncellendi.

**Bulgu — Playwright'ın `devices` preset'leri paketin içinde ama WebKit'e geçiyor, CI sadece Chromium kuruyor.** Agent'ın taraması: `@playwright/test` (`^1.49.1`) `devices` export'unu zaten içeriyor (yeni bağımlılık gerekmiyor) ama `devices['iPhone 13']` gibi hazır preset'ler WebKit'e geçiyor — CI'ın iki job'ı da SADECE `chromium` kuruyor (`playwright install --with-deps chromium`), WebKit preset'i CI YAML değişikliği gerektirirdi. **Karar: `browserName` sabit `chromium`, sadece `viewport:{375,667}` override eden özel bir proje** — CI'a dokunulmadı. `playwright.config.ts`'e `testIgnore`/`testMatch` çiftiyle iki proje eklendi (`desktop` mevcut 62 testi, `mobile-375` sadece yeni `mobile-viewport.spec.ts`'i çalıştırıyor) — mevcut süit İKİYE KATLANMADI, sadece yeni dosyanın testleri eklendi. `playwright.fullstack.config.ts`'e dokunulmadı (RoomHeader taşma riski mock'lanmış login ile zaten hızlı `e2e/` süitinde test edilebiliyor, gerçek DB/API gerekmiyor).

**Uygulama, 3 commit:** (1) `RoomHeader.tsx`/`RoomModerationSection.tsx` `flex-wrap` + `mobile-375` Playwright projesi + `mobile-viewport.spec.ts` (kırmızı→yeşil kanıtlı), (2) `qrcode-terminal`→`qrcode` takası + `TotpSettingsView.tsx` QR görüntüsü + `totp-settings.spec.ts` assertion güncellemesi, (3) bu doküman güncellemesi.

Doğrulama: `apps/web` lint/typecheck/build (Turbopack) hepsi temiz, Playwright 64/64 (mevcut 62 desktop + yeni 2 mobile-375, ikisi de commit sonrası tekrar koşuldu). `apps/api`'ye bu dilimde hiç dokunulmadı. Dal `m6/slice-d-mobile-responsive`, 3 commit, push kullanıcının onayına kalıyor.

### Sıradaki
Slice D tamam. Slice E (`User.totpSecret` alan-seviyesi şifreleme) kendi plan-modu turuyla başlayabilir — kendi başına tam bir tur olarak kalıyor (blast radius büyük, backfill migration gerektiriyor).
