# M9 — Full Bilingual Support (EN/TR runtime switch)

*M7'nin Slice I'i Türkçe→İngilizce UI geçişini (BACKLOG A15) BİTİRDİ — varsayılan artık tamamen İngilizce, ama runtime dil SEÇİMİ yok. M9, kullanıcının asıl istediği şeyi (varsayılan İngilizce, Türkçe opsiyonel, kalıcı, hata mesajları dahil) tam olarak inşa ediyor. Bu KENDİ BAŞINA bir milestone — bir özellik değil, çapraz-kesen bir ALTYAPI değişikliği (her sayfa, her hata mesajı, her test dosyasına dokunuyor).*

**Goal:** Kullanıcı ayarlardan Türkçe seçebiliyor, seçim kalıcı, hata mesajları da dahil her şey seçilen dilde.
**Demo:** Bir kullanıcı ayarlardan Türkçe'yi seçiyor, çıkış yapıp tekrar giriyor, arayüz VE bir hata mesajı Türkçe geliyor.
**Estimated hours:** ~130-180 saat (2026-09-02 kapsam turunda güncellendi,
aşağıya bakın — orijinal ~85-110 saatlik tahmin milestone yazıldığından
beri kod tabanının büyümesiyle güncelliğini yitirmişti). 30 saat/hafta
bütçeyle **~4.5-6 hafta**.

## Out of scope
- Kullanıcı-üretimi içeriğin (oda adı/açıklama, mesajlar) çevrilmesi — asla yapılmıyor, kullanıcının kendi kararı.
- Üçüncü bir dil — sadece EN/TR.

## Acceptance criteria
- [x] `User.locale` kolonu var, giriş yapmış kullanıcı için kalıcı (Slice B).
- [x] Giriş yapmamış ziyaretçi için `localStorage` + tarayıcı dili
      algılama fallback'i var — backend `Accept-Language` DEĞİL, frontend
      `navigator.language` (Slice A'nın kararı, aşağıya bakın; Slice B
      bunu login akışına gerçekten bağladı). **Eksik bulunup düzeltildi
      (2026-09-03):** OTOMATİK algılama vardı ama giriş öncesi bunu
      MANUEL değiştirecek bir kontrol hiçbir yerde yoktu (D1/D2'nin
      locale kaynağı `User.locale`, henüz hesabı olmayan bir ziyaretçi
      için bu YOK) — `AuthPageShell.tsx`'e LandingPage'in AYNI görsel
      TR/EN kutusu eklendi (kod PAYLAŞILMADAN, sadece görsel tutarlılık).
- [x] Backend'in TÜM REST hata fırlatma noktaları makine-okunabilir bir
      `code` taşıyor (Slice C, 2026-09-02) — 71/71 (11 zaten kodluydu + 60
      yeni: 57 mesaj-argümanlı + 3 argümansız `ThrottlerException()`,
      `HttpException`'a çevrilerek kodlandı). WS katmanı
      (`messages.gateway.ts`) bilerek KAPSAM DIŞI bırakıldı — zaten
      neredeyse tamamen kodlu (10/12), kalan 2 nokta küçük bir ek olarak
      açık kalıyor.
- [ ] Frontend, backend'den gelen `code`'u kullanıcının seçtiği dilde bir
      mesaja çeviriyor (düz TS sözlük yaklaşımı — 2026-09-02 kapsam
      turunda kesinleşti, aşağıya bakın). **Mekanizma D1'de kuruldu**
      (`apps/web/lib/error-messages.ts`, 44 benzersiz REST+WS kodu) ve
      TEK bir gerçek tüketicisi var (RoomView'ın WS `exception`
      handler'ı) — REST-taraflı tüketiciler (AuthView vb.) D2+'ın işi.
- [ ] E-postalar (doğrulama, şifre sıfırlama) `User.locale`'e göre gönderiliyor.
- [ ] `apps/web/e2e/**` + `apps/web/e2e-fullstack/**`'teki 787 Türkçe-metin-bağımlı Playwright seçicisi (`getByText`/`getByRole(...name:)`/`getByLabel`/`getByPlaceholder`, 40 spec dosyası — 2026-09-02 kapsam turunda güncel sayıldı) `data-testid`'e taşındı — dilden bağımsız, hangi dil aktifse aktif olsun testler kırılmıyor.
- [ ] Göreli zaman metinleri ("3 gün önce") her iki dilde de doğru (sözlük
      ile çözülmez, ayrı bir formatlama fonksiyonu gerekir).
      `formatRelativeActivity` D1'de bilingual oldu, TEK gerçek tüketicisi
      `RoomSidebar.tsx` (`DiscoverRoomsView.tsx` D2+'ın işi).

## Tasks — kod dilimleri (Claude uygular, her biri kendi plan-modu turu)

- [x] **Slice A — Sözlük dosya yapısı + tip tasarımı + dil algılama
      entegrasyonu.** Tamamlandı (2026-09-02), `feat/i18n-scaffold`
      dalında. Detay Plan notları'nda.
- [x] **Slice B — `User.locale` + iki-katmanlı saklama senkronu.**
      Tamamlandı (2026-09-02), `feat/user-locale-sync` dalında. Detay
      Plan notları'nda.
- [x] **Slice C — Backend hata kodları (60 eksik REST nokta).**
      Tamamlandı (2026-09-02), `feat/rest-error-codes` dalında. Detay
      Plan notları'nda.
- [ ] **Slice D — Frontend sözlük + tüm component'lerin geçişi (31
      dosya — 2026-09-02 kapsam turunda güncellendi, eski "15" değil).**
      ~50-66 saat. Gerçek kapsam bu tahminin de üzerinde çıktığı için
      (bkz. Plan notları) kullanıcıyla ALTYAPI-ÖNCE alt-dilimlere
      bölündü:
      - [x] **D1 — altyapı.** Tamamlandı (2026-09-03),
            `feat/i18n-frontend-foundation` dalında. `dict` prop-drilling
            hattı + hata-kodu eşlemesi + dil ayarları UI'si + bilingual
            `formatRelativeActivity`, birkaç TEMSİLİ dosyada (RoomView,
            TopBar, AccountMenu, SettingsView, CenteredModal, RoomSidebar
            + yeni LanguageSettingsView) uçtan uca kanıtlandı. Detay Plan
            notları'nda.
      - [ ] **D2+ — kalan dosyaların metin taşınması, 5 dalgaya
            bölündü (2026-09-03 kapsam turu).** Her dosya tek tek
            okunarak taze bir sayım yapıldı — gerçek iş kalan **21
            dosya, 226 metin** (milestone'un eski "~28"i D1'in 4 dosyayı
            bitirip 2'sini kısmen bitirdiğini yansıtmıyordu). 5 dosya
            (`AsciiBackground`/`Avatar`/`MessageContent`/`SidePanel`/
            `LegalPageShell`) hiç kullanıcı metni taşımadığı doğrulandı,
            iş gerektirmiyor. `LandingPage.tsx` kapsam DIŞI kalmaya
            devam ediyor (kendi bağımsız `COPY`/`Locale` mekanizması,
            M11b'nin kararı — tekrar teyit edildi). Dalgalar, önerilen
            sırayla:
            - [x] **D2 (Dalga A) — Auth & bağımsız hesap sayfaları.**
                  Tamamlandı (2026-09-03), `feat/i18n-auth-screens`
                  dalında. Detay Plan notları'nda.
            - [x] **D3 (Dalga C) — Hesap/güvenlik ayar panelleri.**
                  Tamamlandı (2026-09-04), `feat/i18n-settings-panels`
                  dalında. Detay Plan notları'nda.
            - [ ] **D4 (Dalga D) — Oda oluşturma/keşif panelleri.**
                  `CreateRoomView.tsx`(8) + `DiscoverRoomsView.tsx`(13)
                  = 21 metin. ~8-9 saat. Küçük, hızlı kazanım — D3'ün
                  AYNI deseni. `DiscoverRoomsView`'in
                  `formatRelativeActivity` çağrısına GERÇEK `locale` bu
                  dalgada bağlanır (bugün varsayılan/parametresiz).
            - [ ] **D5 (Dalga B) — Sohbet çekirdeği + oda listesi.**
                  `ChatPanel.tsx`(15) + `MessageItem.tsx`(21) +
                  `RoomSidebar.tsx`(kalan 10) + `RoomView.tsx`(kalan 3 —
                  satır 753/770/859) = 49 metin. ~19-20 saat. En yüksek
                  regresyon riski (message-edit/delete/grouping,
                  room-announcement-banner, history-pagination
                  spec'lerinin hepsi bu yüzeye bağımlı) — desen A/C/D'de
                  oturduktan SONRA yapılmalı.
            - [ ] **D6 (Dalga E) — Moderasyon yüzeyleri.**
                  `ModerationQueueView.tsx`(19) +
                  `RoomModerationSection.tsx`(23) +
                  `AssignModeratorSection.tsx`(16) = 58 metin. ~19-20
                  saat. Moderatör-only (en az kullanıcı görüyor, düşük
                  aciliyet) ama en KARMAŞIK (rename/duyuru/silme
                  formları+validasyon) — düşük öncelik + yüksek
                  karmaşıklık = son dalga.

            **Toplam ~85-90 saat** — milestone'un orijinal Slice D
            tahmininin (~50-66 saat) ÜZERİNDE, beklenen bir şey (D1
            zaten bu bütçenin ~15-20 saatini altyapıya harcadı, ve
            aşağıdan-yukarı bu tahmin üstten-aşağı ilk tahminden daha
            güvenilir — "kapsam turunda rakamlar büyür" deseni M9'un
            HER turunda tekrarlandı). Saat rakamları milestone'un kendi
            metodolojisiyle (metin sayısı × sabit oran + dosya-başı
            sabit ek yük + dalga-başı doğrulama yükü) üretildi, GERÇEK
            ölçülmüş hıza dayanmıyor — Slice C'nin "~24-31 saat"i TEK
            bir oturumda bitmişti, yani bu rakamlar "kaç tur gerekir"
            tahmini olarak okunmamalı, sadece göreli büyüklük sinyali.
- [ ] **Slice E — E-posta şablonları iki dilli.** ~8-12 saat (DEĞİŞMEDİ — 4 şablon, kapsam turunda doğrulandı, hiç büyümemiş). `EmailService`'in şablonları `User.locale`'e göre seçilir.
- [ ] **Slice F — Playwright seçicilerinin `data-testid`'e taşınması (787
      nokta, 40 spec dosyası — 2026-09-02 kapsam turunda güncellendi,
      eski "388" değil).** ~37-49 saat (güncellendi, ~2x büyüdü). Mekanik ama büyük hacimli — her `getByText`/`getByRole(...name:)`/`getByLabel`/`getByPlaceholder` çağrısı ilgili elemana eklenen bir `data-testid`'e geçirilir, testin kendisi davranışsal olarak DEĞİŞMEZ.

## Risks
- Bu, projenin şimdiye kadarki EN GENİŞ yayılımlı değişikliği — neredeyse her dosyaya dokunuyor. Tek bir uzun sürede değil, kendi içinde net checkpoint'lerle (Slice C bitince backend testi, Slice D bitince frontend testi) ilerlenmeli.
- Slice F (testid geçişi) M7 Slice I ile ÇAKIŞMAMALI — M7 sadece DEĞİŞEN dosyaların seçicilerini günceller, TAM geçiş burada.
- 85-110 saatlik tahmin kullanıcının kendi ön-analizinin (4 gizli maliyet) doğrulanmış hali — küçümsenmedi. **2026-09-02 kapsam turunda 130-180 saate güncellendi** (aşağıya bakın) — kullanıcının orijinal metodolojisi hâlâ geçerli, sadece kod tabanının büyüklüğü milestone yazıldığından beri değişti.

## Plan notları

### Kapsam turu (2026-09-02)

M9 milestone dosyası M13/M11c gibi bu oturumda YAZILMADI — daha önceki
bir kapsam turunda (`docs/BACKLOG.md`'nin "G. 2026-08-29 KAPSAM TURU"
bölümü) zaten var olan bir milestone olarak doğrulanmıştı ("Madde 3
zaten M9'un tamamı"). Bu tur, Slice A'nın kendi açık kararını (kütüphane
vs. sözlük) netleştirmek + milestone'un rakamlarının hâlâ doğru olup
olmadığını kod okuyarak doğrulamak için yapıldı.

**Kod okunarak doğrulanan güncel rakamlar (hepsi grep+manuel doğrulama
ile, tahmin YOK):**
- Hiçbir i18n kütüphanesi (next-intl vb.) zaten yüklü DEĞİL, `User.locale`
  alanı yok, `Accept-Language` hiçbir yerde okunmuyor — milestone'un
  "sıfırdan başlıyoruz" varsayımı doğru.
- Backend REST hata fırlatma noktası: **68** (milestone'un "52"si,
  M7b/M10/M11a-c'nin büyümesiyle güncelliğini yitirmiş). İlk kaba grep
  "21 kodlu" buldu ama bu YANLIŞ ÇIKTI — `code:` string'i `totp.service.ts`/
  `invites.service.ts`'te İLGİSİZ alanlara (TOTP kodu parametresi, davet
  kodu alanı) denk geliyordu. Çok satırlı, `throw new \w+Exception\(\s*\{
  ...code:` desenine daraltılan İKİNCİ bir grep'le düzeltildi: **11**
  gerçek kodlu REST throw (`auth.service.ts` 8, `jwt-auth.guard.ts` 1,
  `password-policy.service.ts` 1, `users.service.ts` 1) — kalan **57**
  eksik (milestone'un "47"sinden biraz daha büyük, ama İKİYE KATLANMAMIŞ).
  Bu, "ilk bulduğun sayıya güvenme, tekrar doğrula" dersinin kendisiydi.
- **Yeni bulgu, milestone'da hiç yoktu:** WS katmanı (`messages.gateway.ts`'in
  `handleMessageSend`/`handleMessageEdit`'i) zaten NEREDEYSE TAMAMEN
  kodlu (10/12 `WsException` fırlatma noktası `{status,code}` şeklinde —
  `RATE_LIMITED`/`MESSAGE_TOO_LONG`/`MUTED`/`ROOM_ARCHIVED`/
  `ROOM_ACCESS_DENIED` vb., M5/M7a/M11c'nin kendi çalışmalarından
  organik olarak birikmiş). Slice C'nin gerçek işi neredeyse tamamen
  REST katmanında.
- Frontend component dosyası: **31** (`apps/web/app/components/*.tsx`,
  milestone'un "15"inin YAKLAŞIK İKİ KATI).
- Playwright metin-bağımlı seçici: **787** (`e2e/`'de 651 + `e2e-fullstack/`'te
  136, 40 spec dosyası — milestone'un "388"inin YAKLAŞIK İKİ KATI).
- E-posta şablonu: **4** (`EmailService`, milestone yazıldığından beri
  DEĞİŞMEMİŞ — hepsi hâlâ inline Türkçe HTML string, Slice E'nin kapsamı
  aynı kalıyor).

**Kararlar (`AskUserQuestion` ile netleşti, hepsi önerilen seçenek):**
1. **Çeviri altyapısı: düz TS sözlük, next-intl DEĞİL.** Gerekçe: proje
   zaten YAGNI duruşunu koruyor (`useDismissableMenu.ts`), sadece EN/TR
   (3. dil yok) için next-intl'in ICU/pluralization gücü muhtemelen hiç
   kullanılmayacak, yeni bir bağımlılık+öğrenme eğrisi gerekçesiz olurdu.
2. **Saat tahmini ŞİMDİ güncellendi** (yukarıdaki Tasks/Estimated hours
   bölümlerine yazıldı) — Slice C/D/F ölçekle BİREBİR büyüdü (D/F ~2x,
   C ~1.2x), Slice A/B/E ölçekten bağımsız (A küçüldü — kütüphane kararı
   artık kapandığı için ekstra araştırma gerekmiyor, B/E aynı kaldı).

**Sonraki adım:** Slice A'nın (artık sadece sözlük dosya yapısı + tip
tasarımı + dil algılama entegrasyonu, kütüphane karşılaştırması
GEREKMİYOR) kendi plan modu turu — kullanıcı ne zaman isterse.

### Slice A kapsam turu + implementasyonu (2026-09-02) — tamamlandı

Plan modunda 3 paralel Explore agent'ıyla (mevcut TR/EN emsalleri:
`LandingPage.tsx`/legal sayfalar; backend locale-algılama noktaları;
frontend Context/prop-drilling konvansiyonu) kod TAM okunarak
doğrulandı, sonra `AskUserQuestion` ile 2 açık soru daha kapandı:

1. **Locale akış deseni: prop-drilling** (kod tabanında React Context
   HİÇ kullanılmıyor — `AppShell.tsx`'in `accessToken`'ı bile saf
   `useState`+prop ile akıyor, aynı desen izleniyor, ilk Context
   TANITILMADI).
2. **Slice A'nın derinliği: SADECE iskelet** — hiçbir mevcut bileşene/
   controller'a bağlanmıyor, canlı bağlama Slice B/D'nin işi.

**Araştırmada bulunan, plan tasarımını şekillendiren bulgular:**
- Kod tabanında ZATEN 2 farklı, birbirinden TAMAMEN bağımsız "iki dilli"
  mekanizma var — `LandingPage.tsx`'in kalıcı OLMAYAN (`useState`, sayfa
  yenilenince sıfırlanan), tek-dosyalık `Record<Locale, LocaleCopy>`
  deseni (M11b Slice A, BİLEREK dar kapsamlı — "giriş yap sonrası"na hiç
  uzanmıyor) ve `/privacy` + `/privacy/en` gibi route-tabanlı, dosya
  kopyalamalı legal sayfalar (M7b Slice I2/A15). **İkisi de M9'un
  kapsamı DIŞINDA** — `docs/milestones/M11b-landing-onboarding.md` ve
  `docs/BACKLOG.md` A27 bunu birebir doğruluyor ("giriş yap sonrası her
  zamanki gibi İngilizce (M9'un kapsamı, dokunulmuyor)"). Slice A bu
  ikisine DOKUNMADI, aralarında birleştirme YAPILMADI (kasıtlı ayrı
  kalıyorlar).
- `ApiError` (`apps/web/lib/api.ts`) ZATEN bir `code?: string` alanı
  taşıyor (üretimde `INVALID_TOKEN_CODE` kontrolü var) — Slice C/D'nin
  üzerine ineceği temel hazır, Slice A dokunmadı.
- Backend'de `Accept-Language` okuma hiç YOK, CORS'ta hiçbir engel yok
  (safelisted header) — ama giriş yapmamış ziyaretçi zaten tamamen
  client-render bir SPA'da geziniyor (`AppShell.tsx` client component),
  bu yüzden tarayıcı dili algılaması BACKEND'DE DEĞİL frontend'de
  (`navigator.language`) yaşıyor — `apps/api`'ye bu slice'ta hiçbir
  runtime kod GEREKMEDİ.
- JWT payload'ı bugün `{sub, email}`, TTL 15dk — locale eklenmesi
  (Slice B'nin işi) dil değişince en fazla 15dk'lık bir bayatlık riski
  taşıyor, kabul edilebilir bir trade-off olarak BELGELENDİ, KOD
  yazılmadı (kaynağı olan `User.locale` henüz yok).
- `User` modelinde DATA-MODEL.md'nin iddia ettiği bir `region` alanı
  GERÇEKTE YOK (doküman/şema driftı) — bu planın kapsamı dışında, sadece
  not düşüldü.

Uygulama, `feat/i18n-scaffold` dalında (main'den, M9'un diğer
slice'larından BAĞIMSIZ) 2 commit — **sadece 2 yeni dosya, mevcut hiçbir
dosyaya dokunulmadı:**
- `d24c6e9` — `apps/web/lib/i18n.ts` (`Locale` tipi, `DEFAULT_LOCALE`,
  `Record<Locale, Dictionary>` sözlük iskeleti — bir "common"
  namespace'iyle tasarımı kanıtlıyor, `detectBrowserLocale()`) +
  `apps/api/src/db/locale.constants.ts` (aynı `Locale` tipi +
  `isValidLocale` tip-koruyucusu, `core-rooms.constants.ts`'in AYNI
  isimlendirme deseninde).
- `d6b507e` — `locale.constants.spec.ts` (aşağıda).

**Bilerek dokunulmayanlar:** `AuthenticatedRequest`/JWT payload'ı,
`UserProfile.locale`, `formatRelativeActivity`'nin iki-dilli hale
gelmesi — hepsi Slice B/D'nin kapsamı, kaynak veri (`User.locale`)
olmadan eklenecek gerçek bir değer yok.

**Doğrulama:** `npm run lint`+`typecheck` temiz (her iki workspace),
`npm run build` başarılı (her ikisi), `apps/api` birim 333/333 (+4 yeni)
+ e2e 160/160 (etkilenmedi). Doğrulama sırasında yerel Postgres
konteynerinin durduğu fark edildi (`prisma.service.spec.ts`'in 2 testi
"Can't reach database server" ile başarısız oldu) — kod REGRESYONU
DEĞİLDİ, konteyner yeniden başlatılınca 333/333 yeşile döndü (bu
oturumda TEKRARLANAN bir ortam durumu, bkz. STATE.md Tuzaklar).
`apps/web` mock Playwright süiti 146/146 (tek koşum, sağlık kontrolü —
hiçbir UI değişikliği yok, tam iki-koşum kuralı bu slice'ta gerekmiyor).
Görsel doğrulama GEREKMEDİ (hiçbir bileşen sözlüğe bağlanmadı).

### Slice B kapsam turu + implementasyonu (2026-09-02) — tamamlandı

Plan modunda 2 paralel Explore agent'ıyla (backend auth/DTO/endpoint
yapısı, frontend auth akışı/localStorage deseni) kod TAM okunarak
doğrulandı. **Kritik tasarım kararı, araştırmada netleşti:** "tek
seferlik, ilk girişte" senkronu doğru uygulamak için `User.locale`
**NULLABLE** olmalı (varsayılan DEĞİL) — `null` = "hiç açıkça set
edilmedi", ilk `null` iken gelen bir localStorage ipucu senkronlanır;
zaten dolu bir değer VARSA hiçbir ipucu onu ezmez. Bir `@default('en')`
NOT NULL kolon bu ayrımı YAPAMAZDI.

**`AskUserQuestion` ile netleşen açık boşluk:** Milestone'un Slice B/D
tarifi kullanıcının SONRADAN (ayarlardan) dilini değiştirmesi için
hiçbir backend endpoint'ini açıkça hiçbir slice'a atamamıştı — Goal
bunu gerektiriyor ama Slice D "frontend sözlük + component metni
geçişi" olarak tanımlı. **Karar: küçük bir `PATCH /users/me/locale`
endpoint'i Slice B'ye eklendi** (User.locale'in zaten SAHİBİ olduğu için
doğal yer) — Slice D böylece sadece bir UI anahtarını mevcut endpoint'e
bağlayacak.

Uygulama, `feat/user-locale-sync` dalında (main'den, BAĞIMSIZ) 2 commit:
- `5d67721` — `User.locale String?` migration (additive, elle SQL yok).
  `LoginDto.localeHint` (opsiyonel) — `AuthService.login()` locale
  `null`sa TEK SEFERLİK senkronluyor, dolu değeri sessizce yok sayıyor.
  `issueTokenPair`'ın 3. parametresi (`locale: Locale`) — JWT payload'ı
  artık `{sub,email,locale}`, hem login hem refresh çözümlenmiş locale'i
  taşıyor. `JwtAuthGuard` eski (locale claim'i olmayan) token'ları TEK
  bir yerde `?? DEFAULT_LOCALE` ile çözümlüyor (15dk TTL içinde
  kendiliğinden düzelir). `UsersService.getProfile` artık `locale`
  döndürüyor (her zaman çözümlenmiş, JWT bayatlığından bağımsız taze) +
  yeni `updateLocale` + `PATCH /users/me/locale` (`BlocksController`).
  `MeService`'in KVKK dışa aktarımına HAM (null olabilen) `locale`
  eklendi. `apps/web`: `AuthView.tsx` login isteğine `localeHint`
  ekliyor, `RoomView.tsx`'in bootstrap effect'i `profile.locale`'i
  localStorage'a yazıyor (giriş sonrası localStorage sadece bir ayna).
- `9ed60f4` — testler (aşağıda).

**Plan sırasında YAKALANMAMIŞ, implementasyonda bulunan bir test-
izolasyonu tekrarı:** `auth-signup-login.e2e-spec.ts`'in ANA describe'u
`ValidationPipe` KURMUYOR (M6 Slice A'nın kendi bilinçli kararı — kısa
kullanıcı adları gerektiren mevcut testleri kırmamak için) — geçersiz
locale reddi testim İLK yazımda bu ana describe'a eklenmişti, 200 dönüp
başarısız oldu, izole çalıştırmayla bulunup dosyanın KENDİ ayrı
"kanıtlanabilir onay (ValidationPipe)" describe'una taşındı. M11c Slice
A'da AYNI dersin (ValidationPipe konumu varsayılmadan önce doğrulanmalı)
İKİNCİ kez tekrarı — STATE.md Tuzaklar'a eklendi.

**Doğrulama:** `npm run lint`+`typecheck` temiz (her iki workspace),
`npm run build` başarılı (her ikisi), `apps/api` birim 338/338 (+5 yeni)
+ e2e 164/164 (+4 yeni — `blocks.e2e-spec.ts`'in mevcut bir `toEqual`
testi de `locale` alanını içerecek şekilde güncellendi). `apps/web`
mock Playwright süiti 148/148 (İKİ tam koşum — login akışına dokunulduğu
için tam kural uygulandı, +2 yeni), `e2e-fullstack` 10/10 (gerçek DB ile
login akışı uçtan uca doğrulandı). Görsel doğrulama GEREKMEDİ (görünür
bir UI değişikliği yok — Slice D'nin ayarlar anahtarı bunu ekleyecek).

### Slice C implementasyonu (2026-09-02) — tamamlandı

Plan modunda 2 paralel Explore agent'ıyla (kodsuz REST throw noktalarının
TAM envanteri dosya/satır bazında, mevcut 11 örneğin kod şekli+test
konvansiyonu+frontend `ApiError.code` tüketimi) kod TAM okunarak
doğrulandı, sonra doğrudan grep/read ile 2 açık soru netleşti:

- **Toplam sayı 57 değil 60 çıktı** — milestone'un "57"si + 3 argümansız
  `ThrottlerException()` (report/room-creation/room-join throttler
  guard'ları). AC'nin "TÜM hata fırlatma noktaları" ifadesi + WS'in AYNI
  durumu zaten `RATE_LIMITED` ile kodlamış olması yüzünden bu 3'ü de
  DAHIL edildi (`ThrottlerException`'ın constructor'ı obje literal
  ALAMADIĞI için `HttpException({code,message}, 429)`'a çevrilerek).
- **`UserMutedException`** (`messages.service.ts`, `Error`'ı extend
  ediyor) grep ile DOĞRULANDI: SADECE WS gateway'i tarafından
  yakalanıyor, hiçbir REST controller'dan ulaşılamıyor — kapsam dışı
  bırakılması bir TAHMİN değil, kanıtlanmış bir gerçek.

Uygulama, `feat/rest-error-codes` dalında (main'den, M9'un diğer
slice'larından BAĞIMSIZ) 2 commit:
- `281fae2` — 13 kaynak dosyası (11 servis + `auth.controller.ts` + yeni
  `error-codes.constants.ts`) + 4 guard dosyası (`cron-secret.guard.ts`,
  `moderator.guard.ts`, 3 throttler guard'ı). Her `throw new
  XException('mesaj')` mevcut 11 örneğin BİREBİR şekline (`{code,
  message}` obje literal) çevrildi, `message` metinleri DEĞİŞMEDİ. 3
  cross-DOSYA tekrarlanan kod (`USER_NOT_FOUND`, `ROOM_NAME_TAKEN`,
  `INVALID_REFRESH_TOKEN`, sonradan `INVITE_NO_LONGER_VALID` da eklendi —
  auth.service.ts'in signup'taki backstop'uyla invites.service.ts'in
  AYNI Türkçe mesajı paylaştığı fark edildi) `INVALID_TOKEN_CODE`'un AYNI
  gerekçesiyle `error-codes.constants.ts`'e çıkarıldı; geri kalan ~50 kod
  tek-dosyalık kullanım olduğu için INLINE string literal (mevcut
  konvansiyon). **Çapraz-transport tutarlılık kuralı** uygulandı: bir
  REST throw'u WS'in ZATEN kodladığı bir durumla AYNI semantiği
  taşıyorsa (ör. `messages.service.ts`'in "oda arşivlenmiş" throw'ları)
  AYNI code string'i (`ROOM_ARCHIVED`, `ROOM_ACCESS_DENIED`,
  `RATE_LIMITED`) kullanıldı.
- `cf5b0b3` — testler (aşağıda).

**Test stratejisi, planda tarif edildiği gibi:** 60 tekil test EKLEMEK
yerine, HER throw noktası için ZATEN var olan bir `.rejects.toThrow(X
Exception)` / e2e `.expect(4xx)` bulunup `.rejects.toMatchObject({
response:{code:'X'}})` / `response.body.code` kontrolüyle GENİŞLETİLDİ —
60 noktanın NEREDEYSE TAMAMININ zaten bir testi vardı (projenin testing
disiplini sayesinde). Gerçekten hiç test edilmeyen 3 nokta bulundu, YENİ
testler eklendi: `room-moderation.service.ts`'in P2002 backstop'u
(pre-check'in AYRI, hiç test edilmemiş bir yarış-durumu dalı),
`totp.service.ts`'in `disable()` kendi geçersiz-kod dalı
(`confirmEnable`'ınkinden AYRI kod yolu), ve `RoomJoinThrottlerGuard`
(kardeş 2 guard'ın aksine hiçbir e2e/birim testi yoktu — kod tabanında
İLK guard-seviyesi birim test dosyası, `ExecutionContext`'i elle
mock'layarak).

**Doğrulama:** `npm run lint`+`typecheck` temiz, `npm run build`
başarılı. `apps/api` birim 342/342 (+4 yeni), e2e 165/165 (+1 yeni).
İkisinde de birer kez (ayrı ayrı) çalışırken RASTGELE bir dosyada 5000ms
timeout flake'i görüldü (`room-member-backfill.e2e-spec.ts`,
`account-lockout.e2e-spec.ts`) — STATE.md Tuzaklar'ın "uzun oturumda
yerel DB büyür, backfill/timing testleri yavaşlar" maddesiyle BİREBİR
örtüşüyor, izole koşumda ikisi de hemen yeşile döndü, tam süit YENİDEN
koşulunca da temiz geçti — KOD REGRESYONU DEĞİL. `apps/web` DOKUNULMADI
(backend-only slice) — tek bir mock Playwright koşumu (148/148) sağlık
kontrolü olarak yeterli görüldü, `e2e-fullstack` GEREKMEDİ. Görsel
doğrulama GEREKMEDİ.

### Slice D1 kapsam turu + implementasyonu (2026-09-03) — tamamlandı

Slice D'nin gerçek kapsamı araştırmada (3 paralel Explore agent'ı: 31
dosyanın envanteri + AppShell/RoomView prop akışı; backend hata
kodlarının TAM listesi + frontend'in bugünkü `.code`/`.message` gösterim
deseni + WS eşlemesi + `formatRelativeActivity`; ayarlar paneli yapısı +
TotpSettingsView'ın "backend'e PATCH + UI'da anlık yansıma" deseni +
`myProfile` akışı) milestone'un kendi tahmininden (~50-66 saat) de büyük
çıktı: 31 dosya, ~275-300 metin string'i, 9 component için `accessToken`
zincirinden BAĞIMSIZ yeni bir prop-drilling hattı, 44 benzersiz hata
kodu, bugün hiç var olmayan bir dil-ayarları UI'si. `AskUserQuestion` ile
kullanıcı bu kapsamı ALTYAPI-ÖNCE alt-dilimlere bölmeyi seçti — bu turun
kendisi SADECE D1'i (mekanizmayı birkaç temsili dosyada uçtan uca
kanıtlamak) kapsıyor.

**Kilit tasarım kararları:**
- `dict` bir PROP olarak akıyor, `t(key)` fonksiyonu YOK — `RoomView`
  `translations[locale]`'i BİR KEZ türetip child'lara geçiriyor,
  TypeScript'in `Record<Locale,Dictionary>` eksik-anahtar garantisi
  (Slice A'nın kararı) korunuyor.
- Parametreli metinler için küçük bir `interpolate(template, params)`
  yardımcısı (`{ad}` yer tutucuları) — next-intl'in ICU'su GEREKMİYOR,
  Slice A'nın "düz sözlük" kararıyla aynı ruh.
- **EN sözlük değerleri bugünkü metnin BİREBİR kopyası** — Slice F
  (testid geçişi) henüz yapılmadı, 787 metin-bağımlı Playwright seçicisi
  hâlâ aktif. Doğrulama: mock süiti İKİ kez çalıştırıldı, 148/148 →
  150/150 (yeni 2 test), MEVCUT hiçbir test KIRILMADI.
- **`RoomView`'ın WS `exception` handler'ı bir REF'ten okuyor** (
  `localeRef`), kendi closure'ının `locale` değerinden DEĞİL —
  bootstrap effect'i `[accessToken]`'a bağlı (SADECE accessToken
  değişince yeniden çalışıyor), `activeRoomRef`'in AYNI bayat-closure
  gerekçesiyle (satır ~307-314) aynı ref-senkron deseni uygulandı. Bu
  BULUNMASAYDI, kullanıcı dilini değiştirdikten SONRA WS hata mesajları
  ESKİ dilde kalmaya devam ederdi (sessiz bir bug, sadece koddan OKUMA
  yaparak fark edildi, test EDİLMEDEN önce).
- 44 benzersiz hata kodu (`apps/web/lib/error-messages.ts`) `apps/api/src`
  genelinde TAZE bir grep'le (41 REST + 3 WS-özel: MESSAGE_TOO_LONG/
  MESSAGE_INVALID_CONTENT/MUTED) çıkarıldı — "ilk bulduğun sayıya
  güvenme" disiplini burada da uygulandı.

Uygulama, `feat/i18n-frontend-foundation` dalında (main'den, M9'un diğer
slice'larından BAĞIMSIZ) 2 commit:
- `f5f39d1` — `lib/i18n.ts` (Dictionary genişletildi, `interpolate`),
  yeni `lib/error-messages.ts` (`ErrorCode` + `ERROR_MESSAGES` + 
  `translateErrorCode`), `lib/format.ts` (`formatRelativeActivity`
  locale parametresi, opsiyonel/geriye uyumlu), yeni
  `LanguageSettingsView.tsx` (TotpSettingsView'ın deseni: optimistic
  update yok, önce API cevabı beklenir), `SettingsView.tsx` (+"language"
  satırı), `AccountMenu.tsx`+`TopBar.tsx`+`CenteredModal.tsx` (`dict`
  prop'u), `RoomSidebar.tsx` (SADECE dar bir `locale` prop'u, TAM `dict`
  DEĞİL), `RoomView.tsx` (locale/dict türetme, `handleLocaleChange`,
  `getPanelTitle` helper'ı `PANEL_TITLES` statik sabitinin yerini aldı,
  WS handler sadeleşti + 2 eksik dalı otomatik kazandı).
- `6017fe9` — testler (aşağıda).

**Bilerek dokunulmayanlar:** kalan ~28 component dosyasının kendi inline
metinleri (D2+'ın işi), `LandingPage.tsx`'in bağımsız TR/EN mekanizması
(M11b'nin kararı, hiç değişmedi), `AuthView.tsx`'in kendi `err.code`
gösterimi, `ResetPasswordView.tsx`/`VerifyEmailView.tsx` (AppShell
zincirinin dışı), `DiscoverRoomsView.tsx`'in `formatRelativeActivity`
çağrısı (hâlâ varsayılan/parametre olmadan, D2+'da gerçek locale
geçecek).

**Doğrulama:** `npm run lint`+`typecheck` temiz, `npm run build`
başarılı. `apps/web`'de BİRİM TEST YOK (SADECE Playwright e2e,
doğrulandı) — `formatRelativeActivity`'nin TR dalı ve
`translateErrorCode`'un eşlemesi GERÇEK tüketicileri üzerinden e2e ile
kanıtlandı. Mock süiti İKİ kez: 148/148 (mevcut) → 150/150 (+2 yeni,
`language-settings.spec.ts`). `e2e-fullstack` 11/11 (+1 yeni,
`ws-error-messages.spec.ts` — gerçek backend'in zalgo/`hasExcessive
CombiningMarks` doğrulamasını tetikleyip YENİ eklenen
`MESSAGE_INVALID_CONTENT` dalının doğru İngilizce mesajı ürettiğini
kanıtlıyor, room-moderation.service.spec.ts'in AYNI zalgo deseniyle).
Görsel doğrulama: `LanguageSettingsView`'ın ekran görüntüsü alındı,
mevcut panel stil diliyle (terminal estetiği, "✓" işaretli aktif seçim)
tutarlı bulundu.

**Sonraki adım:** D2 (kalan ~28 dosyanın metin taşınması, ilk mantıklı
dalga) — kullanıcı ne zaman isterse, kendi plan-modu turu.

### D2 kapsam turu — 5 dalgaya bölme (2026-09-03)

Kullanıcının açık isteği: kod YAZMADAN, kalan dosyaları mantıklı
gruplara/dalgalara böl, her dalganın tahminini ver, sıra öner. Bir
Explore agent'ıyla `apps/web/app/components/` altındaki 31 dosyanın
TAMAMI tek tek okunarak (D1'in "~275-300" tahminine güvenilmeden, "ilk
bulduğun sayıya güvenme" disiplini) taze bir sayım yapıldı — yukarıdaki
Tasks bölümündeki dalga tablosunun kaynağı bu tur. Öne çıkan bulgular:

- Milestone'un "~28 dosya" rakamı D1'in GERÇEKTE ne kadarını bitirdiğini
  yansıtmıyordu — 4 dosya TAM, 2 dosya KISMEN (RoomView'da 3, RoomSidebar'da
  10 metin kalmış) bitmişti. Gerçek iş kalan: 21 dosya, 226 metin.
- 5 dosyanın (`AsciiBackground`/`Avatar`/`MessageContent`/`SidePanel`/
  `LegalPageShell`) HİÇ kullanıcı metni taşımadığı tek tek doğrulandı —
  bunlar hiçbir dalgaya girmiyor, sıfır iş.
- `LandingPage.tsx`'in kapsam-dışılığı bir kez daha teyit edildi (kendi
  `COPY`/`Locale` mekanizması, `dict` sisteminden bağımsız) — YENİ bir
  karar değil, M11b/Slice A/D1'in kararının tekrar doğrulanması.
- `RoomView.tsx`'te D1'in bıraktığı bir TUTARSIZLIK bulundu: satır 770
  `"Message could not be sent, try again."` hardcoded kalmış, oysa AYNI
  anlamdaki `dict.chatPanel.messageCouldNotBeSent` satır 661'de zaten
  kullanılıyor — D5 (Dalga B)'de bu ikisi birleştirilecek.

**Sıra gerekçesi:** A (auth, en basit+en görünür) → C (ayar panelleri,
izole/düşük risk) → D (oda panelleri, hızlı kazanım) → B (sohbet
çekirdeği, en yüksek regresyon riski — desen oturduktan sonra) → E
(moderasyon, en az kullanıcı+en karmaşık — son).

Bu tur SADECE docs — kod/branch/PR yok, `docs/m9-slice-d2-scope`
dalında tek bir docs-only commit. **Sonraki adım:** D2 (Dalga A: auth &
bağımsız hesap sayfaları) — kullanıcı ne zaman isterse, kendi plan-modu
turu.

### D2 (Dalga A) implementasyonu (2026-09-03) — tamamlandı

6 dosya (`AuthView.tsx`, `AuthPageShell.tsx`, `ResetPasswordView.tsx`,
`VerifyEmailView.tsx`, `PasswordInput.tsx`, `AppShell.tsx`) TAM
okunarak tasarım netleşti:

- **Tek locale-çözümleme noktası: `AppShell.tsx`.** Render gövdesinde
  (state DEĞİL) `readStoredLocale() ?? detectBrowserLocale()`'i BİR KEZ
  çözüp `dict`+`locale`'i `AuthPageShell`'e VE `AuthView`'a AYRI AYRI
  prop olarak geçiriyor (children pattern otomatik taşımıyor).
  `AuthView`'ın kendi eski `readStoredLocale()` çağrısı `locale` PROP'una
  indirgendi — tek kaynak. `ResetPasswordView`/`VerifyEmailView`
  (AppShell zincirinin dışı, kendi route'ları) AYNI çözümlemeyi KENDİ
  içlerinde tekrarlıyor (kabul edilebilir küçük kod tekrarı).
- **`PasswordInput.tsx`'in `dict` prop'u OPSİYONEL** — 6 çağırandan
  sadece 2'si (AuthView, ResetPasswordView) bu dalgada migrate edildi,
  kalan 4'ü (CreateRoomView/DiscoverRoomsView/DeleteAccountView/
  AssignModeratorSection, D3/D4/D6'nın işi) `dict` GEÇMEDEN bugünkü
  İngilizce fallback'e düşüyor — davranışları sıfır değişti.
- **`AuthView`/`ResetPasswordView`'ın hata gösterimi `translateErrorCode`'a
  (D1) bağlandı, basitleşti.** `EMAIL_NOT_VERIFIED` için ayrı bir dal
  ARTIK GEREKMİYOR — D1'in sözlük değeri AuthView'ın eski hardcoded
  override'ıyla ZATEN birebir aynıydı. **Bu, GERÇEK bir davranış
  düzeltmesiydi:** varsayılan (İngilizce) locale'de bile kullanıcılar
  `INVALID_CREDENTIALS`/`INVALID_RESET_TOKEN` gibi kodlarda backend'in
  HAM Türkçe mesajını görüyordu — tam da M9'un var oluş nedeni.
- **3 string `dict.common`'a çıkarıldı** (`connectionError`/
  `backToLogin`/`invalidLink`) — 3 dosyada BİREBİR aynı metin, D2 kapsam
  turunun kendi bulduğu "RoomView 770 tutarsızlığı" dersiyle aynı
  disiplin.

Uygulama, `feat/i18n-auth-screens` dalında (main'den, BAĞIMSIZ) 2 commit:
- `3b8a81d` — `lib/i18n.ts` (`authPageShell`/`authView`/`resetPassword`/
  `verifyEmail`/`passwordInput` namespace'leri + `common`'a 3 paylaşılan
  anahtar), 6 component dosyası.
- `33c60c5` — testler (aşağıda).

**Doğrulama sırasında bulunan, GERÇEK bir davranış değişikliğinin doğal
sonucu olan 3 mevcut test güncellemesi** (bilerek, plan onayında zaten
öngörülmüştü): `auth.spec.ts`'in `yanlis_bilgiler_hata_gosterir_totp_
alani_gorunmez`'i (HAM Türkçe metin yerine "Incorrect email or
password." bekliyor artık), `reset-password.spec.ts`'in `gecersiz_
token_hata_mesajini_gosterir`'i (mock'a gerçekçi bir `code` eklendi),
VE **plan sırasında YAKALANMAMIŞ üçüncü bir örnek**:
`e2e-fullstack/delete-account.spec.ts`'in gerçek-backend login-hata
assertion'ı — ilk grep taraması `Şifre hatalı` desenini (büyük Ş)
aradı, `e2e-fullstack`'teki asıl string olan `E-posta veya şifre
hatalı.`'daki KÜÇÜK ş'yi (farklı Unicode karakteri, İngilizce case-
insensitive kuralları Türkçe ı/İ harflerine güvenilir uygulanmıyor)
YAKALAYAMADI — gerçek test koşumuyla (fullstack, mock değil) bulundu.
"İlk bulduğun sayıya/deseni güvenme" dersinin TÜRKÇE karakter büyük/
küçük harf versiyonu — ileride benzer bir grep yapılırken NOT edilmeli.

**Ayrıca, plan onayında ÖNGÖRÜLMEYEN bir ortam tuzağı tekrarı:**
`delete-account.spec.ts` (fullstack) `DEV_USER_2`'yi GERÇEKTEN siliyor
(STATE.md Tuzaklar'da zaten belgeli) — testin İLK (metin bulunamadı)
başarısızlığı bu satırı GEÇTİKTEN SONRA oldu, yani hesap GERÇEKTEN
silindi; düzeltme sonrası YENİDEN koşum reseed YAPILMADAN "DEV_USER_2
bulunamadı" diye BAŞKA bir şekilde başarısız oldu. Reseed+backfill
zinciri tekrar çalıştırılınca (M11a'nın kendi belgelediği AYNI tuzak)
11/11 yeşile döndü — kod regresyonu değildi.

**Doğrulama:** `npm run lint`+`typecheck` temiz, `npm run build`
başarılı. Mock süiti İKİ kez: 150/150 (mevcut) → 153/153 (+3 yeni: TR
locale'de auth ekranının VE AuthPageShell'in KENDİ metinlerinin
Türkçe render ettiğini, `INVALID_CREDENTIALS`'ın backend'in kasıtlı
FARKLI bir ham mesajına rağmen sözlük çevirisini gösterdiğini —
passthrough'la KARIŞTIRILMASIN diye özellikle ayrıştırıldı —
kanıtlıyor). `e2e-fullstack` 11/11 (yukarıdaki reseed turu sonrası).
Görsel doğrulama: TR locale'de signup formunun ekran görüntüsü alındı,
İngilizce sürümüyle birebir aynı düzen/stil, terminal estetiği korunmuş.

**Sonraki adım:** D3 (Dalga C: hesap/güvenlik ayar panelleri) tamamlandı
(aşağıda) — sıradaki D4 (Dalga D: oda oluşturma/keşif panelleri),
kullanıcı ne zaman isterse, kendi plan-modu turu.

### Fix: giriş öncesi dil değiştirme kutusu (2026-09-03)

Kullanıcının bulduğu gerçek eksik: D2'nin AuthView/AuthPageShell'i
`dict`'e bağladı ama giriş öncesi bunu DEĞİŞTİRECEK bir kontrol yoktu —
D1/D2'nin locale kaynağı `User.locale` (backend), henüz hesabı olmayan
bir ziyaretçi için bu kaynak YOK. Kullanıcı `LandingPage.tsx`'in kendi
bağımsız TR/EN kutusunu (M11b Slice A) paylaşıp paylaşmama sorusunu
sordu — araştırma sonucu: **PAYLAŞMA, sadece GÖRSEL deseni taşı**.
LandingPage'in COPY mekanizması bilerek kalıcı-olmayan/bağımsız
(Slice A'nın "next-intl değil düz sözlük" kararıyla aynı ruhta dar
kapsamlı); `dict` ise kalıcı/persistent bir sistem — ikisini
birleştirmek iki farklı tasarımı zorla kaynaştırmak olurdu.

`AppShell.tsx`'in `locale`'i düz bir `const`'tan `useState`'e çevrildi
(lazy initializer, AYNI başlangıç değeri — `readStoredLocale() ??
detectBrowserLocale()`), `AuthPageShell.tsx`'e LandingPage'in BİREBİR
görsel deseni (rol/aria-pressed/stil, KOD paylaşılmadan) eklendi.
Tıklama `storeLocale()`'i (Slice B'den beri var) çağırıyor — `AuthView`'ın
`localeHint`'i (zaten login isteğine gönderiliyor) OTOMATİK doğru
değeri alıyor, backend'e YENİ hiçbir şey gerekmedi. `ResetPasswordView`/
`VerifyEmailView` AYNI eksikliği taşıyor (bağımsız route'lar, hâlâ
sadece otomatik algılama) ama kullanıcının raporu SADECE /app login/
register ekranıyla ilgiliydi — bilerek kapsam dışı bırakıldı, gerekirse
ayrı bir tur.

Uygulama, `fix/auth-locale-toggle` dalında (main'den, BAĞIMSIZ) 2 commit:
`dbf5772` (feat) + `70673f8` (test — dil kutusunun geçiş yaptığını VE
form state'in - email alanına yazılmış bir değerin - toggle sonrası
KORUNDUĞUNU, `AuthView`'ın unmount OLMADIĞINI dolaylı kanıtlıyor).

**Doğrulama:** `npm run lint`+`typecheck`+`build` temiz. Mock süiti İKİ
kez 154/154 (+1 yeni). `e2e-fullstack` 11/11. Ekran görüntüsü: TR kutusu
seçiliyken tüm form Türkçe, LandingPage'in kutusuyla görsel olarak
tutarlı.

### D3 (Dalga C) — Hesap/güvenlik ayar panelleri (2026-09-04)

`TotpSettingsView`/`BlockedUsersView`/`InviteView`/`DeleteAccountView`/
`ProfileView`/`FeedbackView` — hepsi `RoomView.tsx`'in `CenteredModal`'ı
altında render edilen izole panel çocukları (biri bozulursa diğerleri
etkilenmez). Kod okunurken D2 Dalga A'da defalarca görülen AYNI sınıf
gerçek bug'lar tekrar bulundu: birkaç panel backend'in `code` alanına
HİÇ bakmadan ya kendi hardcoded İngilizce metnini ya da backend'in HAM
(her zaman Türkçe) `err.message`'ını gösteriyordu — İngilizce locale'de
bile Türkçe hata görünüyordu. Hepsi AuthView/ResetPasswordView'ın kurduğu
`translateErrorCode(err.code, locale) ?? err.message` desenine geçti:

- `DeleteAccountView`: `else setError("Incorrect password.")` kaldırıldı
  — backend yanlış şifrede `INVALID_CREDENTIALS` döndürüyor, metin artık
  "Incorrect email or password." (AuthView'ın login hatasıyla AYNI kod/
  metin — bu ekranda e-posta alanı olmasa da tek-kaynaklı metni bozmamak
  ayrı bir çeviri satırı bakımlamaktan daha tutarlı bulundu).
- `ProfileView`: `err.status === 404` kontrolü kod-tabanlı desene geçti
  (`USER_NOT_FOUND` → "User not found.", statüye değil koda güveniliyor
  — kodun geri kalanıyla tutarlı).
- `BlockedUsersView` (`handleBlock`): AYNI `USER_NOT_FOUND` çevirisi.
- `TotpSettingsView` (setup/enable/disable, 3 handler): AYNI desen.
- `InviteView`/`FeedbackView`: hata UI'ı yok, sadece metin taşındı.

Ayrıca `ProfileView.tsx`'in `formatJoinDate`'i `"en-US"`'i HARDCODE
ediyordu — TR locale'de bile ay adları İngilizce kalıyordu (önceden
fark edilmemiş gerçek bir bug). `locale` prop'u eklenip
`toLocaleDateString(locale === "tr" ? "tr-TR" : "en-US", ...)`'a
çevrildi, gerçek bir fullstack koşumla ekran görüntüsü alınarak
doğrulandı ("29 Ağustos 2026" doğru render etti).

`dict.common`'a 2 PAYLAŞILAN anahtar eklendi (D2 Dalga A'nın
`connectionError` promosyon ilkesiyle AYNI): `authenticatorCodeLabel`
(AuthView + TotpSettingsView ×2 + DeleteAccountView) ve `emailLabel`
(AuthView + BlockedUsersView) — ikisi de eskiden `dict.authView`'a
özeldi, `AuthView.tsx` de güncellendi. `PasswordInput`'un opsiyonel
`dict` prop'u `DeleteAccountView`'a bağlandı (D2 Dalga A'dan beri
bekleyen iş).

4 mock e2e testinin route'ları backend'in gerçek davranışını EKSİK
yansıtıyordu (`code` alanı yoktu) — D2 Dalga A'daki "Şifre hatalı"
tuzağıyla AYNI kök neden. `delete-account.spec.ts`, `profile-panel.spec.ts`,
`blocked-users.spec.ts`, `totp-settings.spec.ts` düzeltildi.
`e2e-fullstack/delete-account.spec.ts` kontrol edildi — kendi
yanlış-şifre yolunu test etmiyor, bu dilimden etkilenmedi.

Uygulama, `feat/i18n-settings-panels` dalında (main'den) 2 commit:
`d47a7a9` (feat) + `70189af` (test).

**Doğrulama:** `npm run lint`+`typecheck`+`build` temiz. Mock süiti İKİ
kez 154/154. `e2e-fullstack` 11/11 (Postgres kontrolü + reseed + 3
backfill script'i önce çalıştırıldı — `delete-account.spec.ts`
`DEV_USER_2`'yi gerçekten siliyor). TR locale ekran görüntüsü (geçici
bir test dosyasıyla — screenshot sonrası `dev@koqep.local`'ın
`User.locale`'i tekrar `"en"`e çevrilip dosya silindi, paylaşılan dev
DB'de kalıcı iz bırakılmadı).

**Sonraki adım:** D4 (Dalga D: oda oluşturma/keşif panelleri —
`CreateRoomView`, `DiscoverRoomsView`) — kullanıcı ne zaman isterse,
kendi plan-modu turu.
