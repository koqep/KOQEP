# STATE — Projenin canlı durumu

<!-- Bu proje boyunca en kritik dosya. Her session sonunda güncellenir.
     60 satırı geçmesin; geçmiş bilgi docs/decisions/ veya milestone dosyalarına taşınır. -->

**Son güncelleme:** 2026-08-12
**Aktif milestone:** **M7a** (`docs/milestones/M7a-scale-gate.md`) — hedef kullanıcı sayısı 20-30'dan **500**'e çıktı (founder kararı), tam bir kapsam turu yapıldı, M7 SONRA ikiye BÖLÜNDÜ (M7a=kapı açma eşiği ~98-131h/4-5.5hafta, M7b=cila `docs/milestones/M7b-scale-polish.md` ~47-67h/2-3hafta, faz eşiklerine göre sıralı). M6 TÜMÜYLE BİTTİ VE main'e MERGE EDİLDİ (A-G, PR #46/#52/#53/#54/#55/#56, bağımsız doğrulandı). M0-M6 TÜMÜ TAMAMLANDI.

## Şu an ne çalışıyor
- **M0-M6 (tüm milestone'lar) TAMAMEN BİTTİ.** Detaylar kendi milestone dosyalarının Plan notları bölümlerinde.
- **2026-08-12 — KRİTİK KAPSAM DEĞİŞİKLİĞİ: 500-kullanıcı kapsam turu.** 3 paralel araştırma agent'ıyla (altyapı gerçekliği + ateşlenmiş tetikleyiciler, spec'te-olan-ama-yapılmamış özellikler, küçük UX boşlukları + i18n maliyeti) TÜM proje kod okunarak yeniden denetlendi. **Kritik bulgu: `ADR-0002`'nin kendi "httpOnly cookie" kararı hiç tamamlanmamış** — `page.tsx` sadece bellek-içi state kullanıyor, kendi yorumunda bunu "ara adım" diye tanımlıyor. Sonraki turda 4. bir araştırma agent'ıyla TÜM diğer ADR'ler (0003-0008) aynı desen için tarandı — **hiçbiri benzer şekilde yarım kalmamış**, ADR-0002 TEK örnek (0005/0008 hatta kendi ADR'lerine kod-içi referans veriyor). **`RoomMember` üyelik modelinin tetikleyicisi (>50 kullanıcı) fiilen ateşlendi** — bugün her mesaj TÜM bağlı kullanıcılara broadcast ediliyor (`messages.gateway.ts:84-98,251-268`), üyelik yok. Yeni milestone'lar: **`M7a`+`M7b`** (M7'nin bölünmüş hali, aşağıya bakın), `M8` (sosyal özellikler: DM/mention/reaksiyon/okundu-durumu vb., ~230-280h, SONRA — reaksiyon/DM/mesaj-arama BİLİNÇLİ ERTELEME olarak `docs/BACKLOG.md`'ye somut tetikleyicilerle işaretlendi, sessiz kayma DEĞİL), `M9` (tam iki-dilli destek, ~85-110h, SONRA, ayrı çapraz-kesen iş), `M10` (arayüz yeniden tasarımı — kullanıcının kendi "M7"si, sıralama nedeniyle renumbered, iki-aşamalı). `docs/BACKLOG.md`'nin "Karar kuralı (500 kullanıcı dönemi)" + yeni "F. 500-KULLANICI KAPSAM TURU" bölümü, `docs/THREAT-MODEL.md`'nin güncellenmiş satırları + 4 yeni açık madde, `docs/PRD.md`'nin revize "Success metrics"i — hepsi bu turda güncellendi. **Toplam istenen kapsam (M7a+M7b+M8+M9+M10 hepsi) 30 saat/hafta bütçeyle GERÇEKÇİ DEĞİL** (~600-750 saat ≈ 5-6 ay) — sadece M7a (~4-5.5 hafta) gerçekçi bir sonraki adım, gerekçe M7a dosyasının kendisinde.
- **2026-08-12 — M7 İKİYE BÖLÜNDÜ + AŞAMALI DAVET PLANI.** Founder kararı: tek parça M7 (~155-175h, 6-7.5 hafta) boyunca ürünü kimseye gösterememek kapalı-1.0 lansmanın amacıyla (riskli varsayımı erken test etmek) çelişiyor. **`M7a-scale-gate.md`** (~98-131h çekirdek/120-165h tampon, ~4-5.5 hafta) = kapıyı GERÇEKTEN açan kalemler: oturum kalıcılığı, `RoomMember` (mimari istisna — kullanıcı sayısından bağımsız, Faz 0'da bitmeli), self-servis moderatör, hesap sertleştirme, landing/hukuki EN-TR, analitik SQL, yük testi. **`M7b-scale-polish.md`** (~47-67h çekirdek/60-85h tampon, ~2-3 hafta) = cila, Faz 1 açılışını ENGELLEMEZ, kendi faz eşiklerine göre sıralı (rate limit→Faz1-2 arası gerçek veriyle, moderasyon itirazı+zalgo+oda keşfi→Faz 2 öncesi, A15 dil bitirme→Faz 3 öncesi, geri bildirim/kendi-mesaj-silme/düzenlendi-göstergesi/taslak/link→herhangi bir zaman). Aşamalı davet: **Faz 0 (0)→Faz 1 (~50)→Faz 2 (~150)→Faz 3 (~500)**, gerekçe: altyapı/moderasyon yükü TOPLAM sayıyla değil EŞZAMANLI yükle ölçekleniyor, M7b'nin ağır kalemleri tahminle değil gerçek faz verisiyle zamanlanıyor. **"İlk 50 kişiyi ne zaman davet edebilirim?" → M7a bitince, ~4-5.5 hafta.**
- Stack: NestJS (API+WS, Render) + Next.js (Vercel) + Postgres (Render Postgres) + Prisma + Resend + Sentry.

## Şu an üzerinde çalışılan
- **Görev:** 500-kullanıcı kapsam turu + ADR yarım-kalma taraması + M7'nin M7a/M7b bölünmesi + aşamalı davet planı TAMAMLANDI — docs-only, kod yazılmadı. M6 Slice G'nin merge durumu bu tur içinde bağımsız doğrulandı (PR #56, main'de).
- **Sonraki adım:** M7a'nın ilk dilimi — **Slice A, oturum kalıcılığı (ADR-0002'yi bitirmek)** — kendi plan-modu turuyla başlıyor. M10 Faz 1'in M7a Slice B/G için gereken ERKEN tasarım kararları o dilimlerden ÖNCE alınmalı (M7a dosyasının kendi koordinasyon notu).

## Bilinen sorunlar / teknik borç
- `npm audit`: 32 high severity uyarı var, henüz değerlendirilmedi.
- Prisma majör sürüm güncellemesi bekliyor (6.x → 7.x) — şimdilik ertelendi.
- Rate limit sayıları gözden geçirildi (M6 Slice B) — gerçek 5 aktif limit 20-30 kişilik ölçekte yeterli bulundu, DEĞİŞTİRİLMEDİ (detay: milestone dosyası Slice B notları).
- `User.role` alanı var, erişim kontrolü kodda çalışıyor ama production'da henüz kimse `moderator` değil — founder'ın kendi satırını elle SQL ile ayarlaması hâlâ öneriliyor.

## Yakın zamanda alınan kararlar
- DB hosting: Render Postgres, Internal Database URL — Neon/Supabase değil.
- WS transport: Socket.IO — bkz. `docs/decisions/ADR-0007`.
- Access token bellek-içi (React state), localStorage/sessionStorage YOK — bkz. ADR-0002.
- 2026-07-29 — `SEED_DEV_FIXTURES` opt-in env'i `NODE_ENV`'e bilerek güvenilmedi; sıfır-kullanıcılı DB bootstrap boşluğu `docs/THREAT-MODEL.md` Open items'a somut tetikleyiciyle eklendi.
- 2026-07-30 — M2 Slice A-G kararları tekrar buraya taşınmadı — `docs/milestones/M2-core-rooms-messaging.md`'nin Plan notları bölümlerinde tam haliyle duruyor.
- 2026-08-12 — ADR-0008: `totpSecret` şifreleme anahtar-kaybı KALICI (kurtarılamaz), rotasyon yolu YOK (bilinçli, gerekirse ikinci anahtar + re-encryption script'i gerekir) — ikisi de sessiz varsayım değil, ADR'de açıkça yazılı.

## Tuzaklar (Claude buraya düşmesin)
- `docs/BACKLOG.md` boş bir şablon DEĞİL — dolu ve detaylı; kapsam tartışılırken oku.
- `ReputationEvent` sadece insert edilir; mesaj içeriği asla hard-delete edilmez, sadece yazar anonimleştirilir.
- "main güncel" / "merge oldu" iddialarını HER ZAMAN `git fetch` + `git log origin/main` ile bağımsız doğrula (bu oturumda iki kez yapıldı, ikisi de doğru çıktı).
- CI'da job-seviyesi env değişkenleri JOB'LAR ARASI miras YOK — her job'ın kendi `env:` bloğu var, yeni bir env var eklerken HER job'ı tek tek kontrol et. Sahte/placeholder CI değerleri (ör. `RESEND_API_KEY: ci-only-test-key`) sadece TÜM kod yolları hatayı sessizce yutuyorsa güvenlidir; DI ile override edilemeyen canlı süreçlerde sahte'lemek gerekirse kesin string eşitliği kullan (`=== 'fake'`, TRUTHY DEĞİL), SADECE ihtiyaç duyan job'a ekle.
- Prisma client üretimi TEK kaynaktan: `apps/api/package.json`'daki `postinstall` script'i — buildCommand/CI adımına gömmeye çalışma. Render servisi Blueprint'e bağlı DEĞİL, elle kuruldu — `render.yaml` değişiklikleri canlıya OTOMATİK yansımaz; bir env var'ın DEĞERİNİ değiştirmek de yansımayabilir, sadece tamamen SİLMEK işe yaradı.
- `.env`/`.env.*` dosyaları `Read` için engelli (içerik değiştirmek için `Write` ile tam dosyayı körlemesine yeniden yaz); `@prisma/client` import edilince `.env`'i ayrıca sessizce (yeniden) yükler — testte "env var yok" simüle etmek için `delete process.env.X` DEĞİL, boş string (`''`) kullan.
- `resend` SDK hata durumunda fırlatmaz, `{data, error}` döner — elle kontrol şart.
- `apps/web`'de token bellek-içi (`page.tsx`) — reload oturumu düşürür. **DÜZELTME (2026-08-12):** bu "kasıtlı/bitmiş bir karar" DEĞİL — `ADR-0002`'nin kendi "Decision" bölümü httpOnly cookie diyor, kod hiç oraya varmamış, kendi yorumunda "ara adım" diyor. M7 Slice A bunu bitiriyor.
- Sıfır kullanıcılı bir DB'de `/auth/signup` kendi kendini başlatamaz — davetin `issuedById`'i var olan bir `User`'a FK'lidir. İlk kullanıcı her zaman elle `INSERT INTO "User"` + lokal `argon2.hash` gerektirir — "eksik davet kodu" sanıp Invite tablosuna uğraşma, önce User'ı elle yarat.
- Bash tool `git push`'u kullanıcının izin ayarları sessizce "denied" döndürebilir (hata değil) — kullanıcıdan onaylamasını ya da kendisinin push etmesini iste, sessizce vazgeçme. Yerel Postgres Docker container'ı için önce `docker ps` ile çalışıp çalışmadığını kontrol et.
- `prisma migrate dev` migration dosyasını hemen uyguluyor — elle SQL eklemek için önce `--create-only` kullan, YOKSA dosyayı sonradan editlemek checksum uyuşmazlığı yaratır (`prisma migrate reset` gerektirir, kullanıcının TAM onayını ister). `--create-only`, mevcut satırları olan bir tabloya varsayılansız zorunlu kolon eklerken ("nasıl doldurulsun" insan girdisi gerektirdiğinde) non-interactive ortamda TAMAMEN REDDEDİYOR — migration dosyasını elle oluştur (nullable ekle → doldur → sıkılaştır, tek dosyada), sonra `prisma migrate dev` sadece uygular.
- `@nestjs/throttler` iki tuzağı: (1) aynı route'ta HEM global (APP_GUARD) HEM route-özel bir `ThrottlerGuard` alt sınıfı varsa ikisi de AYNI `@Throttle()` metadata'sını okuyup habersiz AYRI sayaç tutar — farklı tracking gereken route'lar için `ThrottlerStorage`'ı doğrudan kullanan bağımsız bir `CanActivate` yaz. (2) `storageService.increment()`'e `blockDuration=0` vermek bloğu AYNI çağrıda sessizce sıfırlıyor — hep `ttl` (ya da üstü) ver.
- `SEED_DEV_FIXTURES`'ın seed'lediği dev kullanıcı Slice B'den beri `emailVerifiedAt` olmadan giriş yapamıyordu (`seed.ts` bunu set etmiyordu) — lokal DB'de migration backfill'i eski satırı zaten doğrulamış olduğu için GÖRÜNMEDİ, CI'ın her koşuda kurduğu SIFIR satırlı taze DB'de create dalı ilk kez çalışıp açığa çıktı. Ders: "CI'da kırmızı, lokalde yeşil" bir e2e/seed sorununu gerçekten doğrulamak için kalıcı lokal DB'ye değil, taze bir throwaway Postgres container'ına karşı test et.
- NestJS WS gateway'lerinde: varsayılan exception filtresi `WsException` DIŞINDAKİ her şeyi jenerik "Internal server error"a çeviriyor (kaynak kodda doğrulandı) — yapısal bir hata sinyali (`code` alanlı) istiyorsan `WsException({code:...})` fırlat. Ayrıca handler `Promise<void>` dönerse ack callback'i (başarıda bile) hiç tetiklenmiyor — gerçek bir değer dönmesi şart; client'ta da düz `.emit(event,payload,cb)` disconnect'te ack'i sessizce kaybeder, `.timeout()` şart. M2.5 Slice D'de üçü de kaynak okunarak doğrulandı, sonra e2e'de kanıtlandı.
- `messages.gateway.ts`'in `handleConnection`'ı bağlantıda hangi odalara `client.join()` edileceğini SABİT bir listeden (`CORE_ROOM_NAMES`) alıyordu — dinamik/kullanıcı-üretimi kaynaklar (M3'te oda) eklerken bu tür "bağlantı anında sabit bir listeye katıl" desenlerini MUTLAKA ara: yeni kaynak var olsa bile hiçbir soket ona hiç katılmaz, gerçek zamanlı teslimat sessizce kırılır. Milestone dokümanının Tasks listesi bunu hiç anmıyordu, sadece kod okuyarak bulundu.
- Plan modundayken `ExitPlanMode` SADECE "implementasyona başla" onayı DEĞİL — plan dosyası dışındaki HER dosyayı düzenlemenin (docs dahil) tek kilidi. Kullanıcı "kod yazma, sadece docs güncelle" dediğinde bile önce `ExitPlanMode` çağrılmalı; aksi halde `Edit`/`Write` plan-modu kısıtlamasına takılır, kullanıcı aynı isteği tekrarlamak zorunda kalır (bu oturumda oldu).
- `Message` interface'i artık İKİ ayrı dosyada tanımlı (`MessageItem.tsx` ve `ChatPanel.tsx`, RoomView.tsx bölme refactor'ünün bilinçli tercihi — import yerine tekrar) — birini değiştirirsen diğerini de kontrol et, sessizce birbirinden sapabilirler.
- Paylaşılan bir transaction içinde (ör. `sendMessage`'ın `$transaction`'ı) N adet satır yazarken bunu bir döngüde N ayrı `create()` ile YAPMA — aynı transaction'ın tuttuğu paylaşılan bir satır kilidini (ör. `Room`) N round-trip kadar uzatır, timeout riski doğurur. Tek bir `createMany` kullan (M4 Slice B'de `InvitesService.grantInvites`'ta bir Plan agent'ının bulduğu gerçek sorun).
- e2e temizlik tekrarlayan tuzaklar: (1) Bir yardımcı fonksiyon gerçek bir servis metodu çağırıp PAYLAŞILAN bir kaynağa (ör. `#general` odası) satır yazıyorsa, o satır da temizlik dizisine eklenmeli — sadece testin doğrudan `prisma.X.create` çağrıları değil (M4 Slice B, paylaşılan oda kalıcı kirlendi). (2) Bir audit-log satırının hedef FK'si (ör. `reportId`) NULL olabiliyorsa ("kuyruk görüntülendi" gibi hedefsiz bir olay), `WHERE targetId IN (...)` temizlik filtresi bunu HİÇ yakalamaz — ayrı bir anahtarla (ör. `actorId`/`moderatorId`) temizle (M5 Slice A/B'de tekrar doğrulandı). (3) Yeni bir tabloya `ON DELETE RESTRICT` FK eklerken (Prisma varsayılanı) `afterAll` temizlik sırası tersten olmalı — ebeveyn (ör. `Message`) çocuk (ör. `MessageEdit`) olmadan silinemez.
- `docs/BACKLOG.md`'deki "somut tetikleyici" notları başka bir milestone'u NUMARAYLA anıyorsa (ör. "M4 şipse") körü körüne güvenme — milestone numaralandırması zamanla kayabilir (moderasyon önce "M4" sonra "M5" oldu), tetikleyici metni güncellenmemiş olabilir. Tetikleyicinin GERÇEKTE hangi milestone'a işaret ettiğini (numaraya değil, konuya bakarak) doğrula. M5'in ikinci tur gözden geçirmesinde "oda moderasyonu" maddesi tam bu yüzden iki milestone kaymıştı.
- Test harness'ın üretim davranışını EKSİK yansıttığı iki durum (M5 Slice B'de bulundu): (1) `apps/web`'in Playwright config'i `next start` kullanıyor (dev DEĞİL) — `.next` build'i kod değişikliğinden SONRA yeniden `npm run build` edilmezse testler ESKİ derlenmiş kodu sessizce test eder. Frontend değiştiren her oturumda Playwright'tan önce build şart. (2) `apps/api`'nin e2e `TestingModule`'ü `main.ts`'in `app.useGlobalPipes(new ValidationPipe(...))`'unu HİÇ çalıştırmaz — hiçbir e2e dosyasında DTO validasyonu (400) gerçekten test edilmiyor; kendi dosyanda test etmek istiyorsan `beforeAll`'a aynı pipe'ı elle ekle.
- Playwright'ta iki tekrarlayan tuzak (M5 Slice D'de bulundu): (1) `page.route("**/rooms", ...)` sondaki `**` OLMADIĞI için `?includeArchived=true` gibi bir query string'i eşleştirmez (istek gerçek ağa düşer, sessizce asılı kalır) — bir endpoint hem sorgusuz hem sorgulu çağrılıyorsa deseni `"**/rooms*"` yap. (2) `getByRole("button", {name:"sil"})` `exact:true` OLMADAN SUBSTRING eşleşir — sayfada başka bir yerde "hesabı sil" gibi metni İÇEREN bir buton varsa yanlışlıkla onu da bulur, sayım/görünürlük assertion'larını bozar.
- `apps/api/eslint.config.mjs`'de `dist/**` ignore YOKTU (M6 Slice B'de bulundu) — `npx eslint .` bir build'den sonra derlenmiş `dist/` çıktısını da taramaya çalışıp 112 "parsing error" üretiyordu. `npx eslint . 2>&1 | tail -N` bunu GİZLER (pipeline exit code'u `tail`'inki, eslint'inki değil) — lint'i pipe'lamadan çalıştır ya da exit code'u AYRI yakala (`cmd > file 2>&1; echo $?`).
- Yerelde CI'ı tam adımlarıyla taklit ederken iki tuzak (M6 Slice D'de bulundu): (1) Windows'ta `npx playwright test` bazen kök `.gitignore`'a `test-results/` satırını UTF-16 ile ekliyor - `git diff` "Binary files differ" gösterir, commit'e girmeden `git checkout -- .gitignore` ile fark edilip geri alınmalı. (2) Kalıcı bir yerel dev DB'ye (CI'ın throwaway DB'sinin aksine) karşı aynı testi tekrar tekrar koşmak eski test verisi biriktirir, `getByText` gibi substring-eşleşen assertion'ları strict-mode ihlaliyle YANLIŞ KIRMIZI yapabilir - CI'daki gerçek bir başarısızlıkla karıştırılmamalı, önce eski satırlar temizlenmeli.
